name: Dependency update

# One batched Gradle dependency PR per week, as a REUSABLE workflow — the JVM
# sibling of npm-update, consumed with:
#
#   uses: mikelward/gradle-update/.github/workflows/gradle-update.yml@main
#
# from a small caller workflow that owns the cron. See README.md for the
# caller shape and the permissions it must grant.
#
# update-versions.mjs moves every entry in gradle/libs.versions.toml to the
# newest stable release WITHIN ITS CURRENT MAJOR, so a direct dependency
# cannot cross a major: the publish job re-derives that from the diff
# (check-gradle-update.mjs) and stops the run if one did. Majors stay a
# deliberate, human-initiated migration.
#
# Beneath the catalog the promise is weaker than npm-update's: these repos
# have no Gradle lockfile, so a same-major direct bump can pull a new
# TRANSITIVE major with nothing in the catalog diff to show for it. The
# consumer's own test suite (run here) and its dispatched CI are the coverage
# for that. Read "no majors" as guaranteed for what the catalog declares,
# best-effort beneath it.
#
# The trust split is inherited from npm-update wholesale: resolving versions
# reads Maven METADATA only and executes no dependency code, but the Gradle
# checks that validate the batch execute dependency and plugin code by
# definition. So the update job holds no write token, the manifests are
# fingerprinted before Gradle runs, and the publish job — a fresh runner that
# installs nothing — is where the diff is actually validated and pushed.

on:
  workflow_call:
    inputs:
      catalog:
        description: Path to the Gradle version catalog.
        type: string
        default: gradle/libs.versions.toml
      cooldown-days:
        description: >-
          Minimum age in days of a release before this job will take it — the
          Gradle analog of .npmrc's min-release-age, so a compromised release
          has time to be yanked before an unattended job picks it up.
        type: number
        default: 5
      extra-repositories:
        description: >-
          Additional Maven repository URLs to resolve versions from, one per
          line, https only. APPENDED to Maven Central, Google Maven and the
          Gradle Plugin Portal — never replacing them, since a consumer still
          needs those for everything that is not its own. Declare the same
          repository in the consumer's settings.gradle.kts too, or the batch
          will select versions Gradle cannot resolve. An entry here is
          trusted for version selection exactly as much as Maven Central is,
          and the publish job re-asks the same host rather than an
          independent one.
        type: string
        default: ''
      no-cooldown-for:
        description: >-
          Maven coordinates (group:artifact, one per line) exempt from the
          release-age cooldown. For a repository you publish yourself the
          window guards nothing — it exists so a compromised THIRD-PARTY
          release has time to be yanked — and only adds latency between your
          release and the batch that takes it. A key is exempt only when
          EVERY coordinate it pins is declared here, so a shared key spanning
          your own artifact and a third-party one keeps the full window.
          Waived keys are named in the PR body rather than moving silently.
        type: string
        default: ''
      java-version:
        description: JDK major for the Gradle checks.
        type: string
        default: '21'
      checks:
        description: >-
          Commands that validate the batch, one per line. They run in the
          update job, after the catalog is rewritten and fingerprinted.
        type: string
        default: |
          ./gradlew test
          ./gradlew lint
      review-checks:
        description: >-
          Commands that flag the batch for HUMAN review, one per line — the
          consumer's license-diff check is the motivating case. They run
          after the checks and the regenerate commands, in the same
          untrusted window; any nonzero exit marks the PR "needs human
          review" (title and body) and leaves auto-merge unarmed, without
          failing the batch, and what the command printed is captured into
          the body's Review section as the reason. Tracked files a review
          check modifies are diffed into the report and restored — review
          checks observe, they never publish; a file the batch should
          publish belongs in regenerated-files instead. Empty disables.
        type: string
        default: ''
      regenerate:
        description: >-
          Commands that rebuild files derived from the dependency set (a
          bundled-license inventory, say), one per line. They run after the
          checks, in the same untrusted window; a failure stops the run,
          because publishing a batch whose derived files are stale is the
          silent drift this input exists to end. Empty disables.
        type: string
        default: ''
      regenerated-files:
        description: >-
          Tracked files the regenerate commands rebuild, one per line —
          committed with the catalog when they change, fingerprinted and
          verified the same way. The declaration is the boundary: only
          files named here may change during regeneration, and anything
          else the commands touch stops the run exactly as the tree check
          always has. Required when regenerate is set.
        type: string
        default: ''
      commit-prefix:
        description: >-
          Optional subject prefix for the batch commit and PR title, e.g.
          'internal:' or 'deps:'. Empty by default: a dependency bump does
          change the shipped build, and on the Android repos the commit
          subject ships straight to Play/Firebase release notes, so the
          batch commit is bare like any other release-worthy change.
        type: string
        default: ''
      ci-workflow:
        description: >-
          Workflow file to dispatch against the pushed branch, with a `pr`
          input naming the pull request. A pull request this job opens
          under GITHUB_TOKEN's identity DOES trigger a consumer's own
          `on: pull_request` workflows, same as any other — but GitHub gates
          that run pending manual approval, since GITHUB_TOKEN's identity
          (`github-actions[bot]`) is not a repository collaborator; nothing
          runs until someone clicks approve. Dispatch sidesteps the gate
          (see docs/PAT.md or docs/GITHUB_APP.md for the version-controlled
          fix: a real user or a GitHub App installation, either of which IS
          a collaborator). It must carry
          `workflow_dispatch` on the default branch. Empty disables the
          dispatch.
        type: string
        default: android-ci.yml
      environment:
        description: >-
          The deployment environment the publish job runs in, and so the
          environment whose secrets it can read. The batch credential —
          GRADLE_UPDATE_PAT, or GRADLE_UPDATE_APP_ID plus
          GRADLE_UPDATE_APP_PRIVATE_KEY — belongs there, not among the
          repository's secrets: a repository secret passed to this workflow
          reaches the runner of EVERY job in it, the update job included,
          where plugin and dependency code runs with sudo, while an
          environment secret reaches only the job that declares the
          environment, and the update job never does. Pair it with
          `secrets: inherit` on the caller; see docs/PAT.md.
        type: string
        default: gradle-update
    # Passing a secret through this block is the LEGACY route — see the
    # `environment` input above: a secret handed to the workflow this way
    # reaches every job's runner, the untrusted update job included. Kept so
    # a caller written before the environment existed keeps working; the
    # publish job prefers the environment-scoped, hub-named secret whenever
    # the caller passes `secrets: inherit`, and falls back to these.
    secrets:
      app-id:
        description: >-
          A GitHub App's ID, for minting a short-lived installation token to
          push the branch and open the pull request with instead of
          GITHUB_TOKEN — see docs/GITHUB_APP.md. Optional; omit both this
          and app-private-key to keep using GITHUB_TOKEN. Providing one
          without the other is refused: a partial credential mints no
          token, and failing loudly beats silently falling back to the
          weaker one.
        required: false
      app-private-key:
        description: The App's private key (the full .pem contents), paired with app-id.
        required: false
      token:
        description: >-
          A personal access token to push the branch and open the pull
          request with instead of GITHUB_TOKEN — see docs/PAT.md. Simpler
          one-time setup than app-id/app-private-key, at the cost of tying
          the weekly PR to a real user account rather than a scoped,
          independently-revocable App installation; see docs/PAT.md's note
          on reconsidering this once a repository takes external
          contributions. Optional; omit to keep using GITHUB_TOKEN (or
          app-id/app-private-key, if those are supplied instead). Takes
          priority over app-id/app-private-key if both are set.
        required: false

# Explicitly empty, not omitted: the CALLER grants the real permissions
# (contents: write, pull-requests: write, actions: write) and every job below
# declares its own downscoped block, so this line grants nothing itself — it
# exists so a future job that forgets its own block gets no ambient token
# instead of silently inheriting whatever the caller happened to grant.
permissions: {}

jobs:
  # Rewrites the catalog and runs the checks. Gradle executes third-party
  # plugin and dependency code here, so this job holds no write token and
  # cannot push anything.
  update:
    runs-on: ubuntu-latest
    # The checks run unreviewed dependency code; without a bound, a hung
    # Gradle build holds the runner for the 6-hour job default.
    timeout-minutes: 90
    permissions:
      contents: read
    outputs:
      changed: ${{ steps.changed.outputs.changed }}
      checks_sha: ${{ steps.checks.outputs.checks_sha }}
      review_verdicts: ${{ steps.review.outputs.verdicts }}
      regen_sha: ${{ steps.regen.outputs.files_sha }}
      regen_hash: ${{ steps.regen.outputs.files_hash }}
      regen_mode: ${{ steps.regen.outputs.files_mode }}
      base: ${{ steps.base.outputs.sha }}
      catalog_sha: ${{ steps.changed.outputs.catalog_sha }}
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # v5.0.1
        with:
          # Always the default branch, never the dispatch ref: a manual run
          # from a feature branch must not base the dependency branch on it.
          # (If the expression is ever empty, checkout falls back to the
          # default branch anyway, so the failure mode is benign.)
          ref: ${{ github.event.repository.default_branch }}
          # The Gradle checks execute unreviewed code; the token is read-only,
          # and dropping it from .git/config keeps one less credential in
          # reach anyway.
          persist-credentials: false

      # Pinned and handed to the publish job, so a commit landing on the
      # default branch while the checks run cannot be silently reverted by
      # publishing a catalog resolved against the older base.
      - name: Record the base commit
        id: base
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      # The engine, from this repository at the exact revision of the
      # workflow that is running — `job.workflow_sha` — never a moving ref.
      # That is the commit the caller's `uses:` resolved, whichever ref it
      # named, so a consumer piloting a branch runs THAT branch's engine
      # rather than main's, and the publish job pins the validator to the
      # same revision below without this job having to report which one it
      # used. It used to: the sha traveled as a job output, and a job output
      # is something this untrusted job writes — a forged one would have
      # pointed publish, with the write credential in its env, at any commit
      # reachable from this repository, a fork's pull request head included.
      - name: Fetch the update engine
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # v5.0.1
        with:
          repository: mikelward/gradle-update
          ref: ${{ job.workflow_sha }}
          path: .gradle-update
          persist-credentials: false

      # Reads Maven metadata over HTTPS and rewrites [versions] in place.
      # No dependency code executes here — that starts with the Gradle
      # checks below, after the fingerprint. The catalog path travels as an
      # environment variable here and in every later shell step: inlining it
      # into single-quoted script text would break on an apostrophe in a
      # custom path.
      - name: Resolve updates within their majors
        env:
          CATALOG: ${{ inputs.catalog }}
          # Typed as a number so it cannot carry shell syntax, but env is the
          # discipline every input here travels by — data, never script text.
          COOLDOWN_DAYS: ${{ inputs.cooldown-days }}
          EXTRA_REPOSITORIES: ${{ inputs.extra-repositories }}
          NO_COOLDOWN_FOR: ${{ inputs.no-cooldown-for }}
        run: |
          set -euo pipefail
          # Every comparison this workflow makes against the catalog path —
          # the reserved-name guard below, the tree check's literal
          # allowlist, the adoption diff — is a string comparison, so the
          # path must arrive in canonical workspace-relative form: a spelled
          # variant like `./gradle/…` names the same file while matching
          # none of the strings. Refuse anything non-canonical (with the
          # canonical spelling in the message) rather than guessing which
          # comparisons the variant would slip past.
          canon=$(realpath -m --relative-to="$PWD" -- "$CATALOG")
          if [ "$canon" != "$CATALOG" ]; then
            echo "::error::catalog path '$CATALOG' is not in canonical repo-relative form; use '$canon'."
            exit 1
          fi
          # The run writes these files and directories at the workspace root.
          # A catalog named like one of them would be clobbered by the run's
          # own output (the engine's report, most directly); one NESTED under
          # such a name means the checkout already occupies the name with a
          # directory the run cannot write over — and a catalog under the
          # engine or artifact directories would be deleted by their cleanup.
          # All of it is the same class, refused up front with the reason,
          # as exact names AND as path prefixes. `..` leaves the repository,
          # which no consumer's catalog does.
          case "$CATALOG" in
            ..|../*|report.md|checks.md|review.md|deps-stat.txt|handoff-catalog.toml|body.md|.gradle-update|.dependency-update|\
            regen-stat.txt|handoff-regenerated.tar|\
            report.md/*|checks.md/*|review.md/*|deps-stat.txt/*|handoff-catalog.toml/*|body.md/*|.gradle-update/*|.dependency-update/*|\
            regen-stat.txt/*|handoff-regenerated.tar/*)
              echo "::error::catalog path '$CATALOG' collides with a file or directory this workflow uses, or leaves the repository; choose a different catalog path."
              exit 1;;
          esac
          # The scratch names must also not be files the CONSUMER tracks:
          # the run would overwrite them before the checks read them, and
          # the tree check deliberately allowlists these names, so the
          # clobber would pass unseen. The engine and artifact directories
          # are the same class through a different mechanism — their cleanup
          # (`rm -rf`) would delete tracked content and the tree check would
          # abort every run on the deletions. Same answer: refuse with the
          # reason.
          tracked=$(git ls-files -- report.md checks.md review.md deps-stat.txt body.md handoff-catalog.toml .gradle-update .dependency-update regen-stat.txt handoff-regenerated.tar)
          if [ -n "$tracked" ]; then
            echo "::error::this workflow writes scratch files at the workspace root that this repository tracks:"
            echo "$tracked"
            echo '::error::rename or move those files to run the weekly update.'
            exit 1
          fi
          node .gradle-update/update-versions.mjs \
            --catalog "$CATALOG" \
            --cooldown-days "$COOLDOWN_DAYS" \
            --extra-repositories "$EXTRA_REPOSITORIES" \
            --no-cooldown-for "$NO_COOLDOWN_FOR" \
            --markdown report.md
          # The engine has done its work; removing it keeps the tree check
          # below simple and honest.
          rm -rf .gradle-update

      - name: Stop early if nothing moved
        id: changed
        env:
          CATALOG: ${{ inputs.catalog }}
          # The catalog path is data everywhere, including as a git pathspec:
          # without this, a filename carrying pathspec syntax (`*`, `?`)
          # would glob instead of naming the one file.
          GIT_LITERAL_PATHSPECS: '1'
        run: |
          set -euo pipefail
          if git diff --quiet -- "$CATALOG"; then
            echo 'changed=false' >> "$GITHUB_OUTPUT"
            echo 'No dependency updates available this run.' >> "$GITHUB_STEP_SUMMARY"
          else
            echo 'changed=true' >> "$GITHUB_OUTPUT"
            git diff --stat -- "$CATALOG" > deps-stat.txt
            # Fingerprint now, before any Gradle code runs. A step OUTPUT
            # rather than a file: the runner captures it when this step ends,
            # and nothing that runs afterwards can reach back and change it.
            echo "catalog_sha=$(sha256sum -- "$CATALOG" | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"
            git diff -- "$CATALOG" >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Set up JDK
        if: steps.changed.outputs.changed == 'true'
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654  # v5.2.0
        with:
          distribution: temurin
          java-version: ${{ inputs.java-version }}

      - name: Set up Gradle
        if: steps.changed.outputs.changed == 'true'
        uses: gradle/actions/setup-gradle@4c125117fe7c5aed11272ec4213f602f012f89f2  # v5
        with:
          # This job runs unreviewed dependency code; it must never write the
          # shared Gradle cache — same rule the consumers' own CI applies to
          # non-main refs.
          cache-read-only: true

      # A PR opened by GITHUB_TOKEN triggers no `on: pull_request` workflows
      # (GitHub's loop-prevention rule), so the consumer's CI will NOT run on
      # the update PR by itself: the checks happen HERE or not at all. Every
      # check runs even after one fails, so the PR body reports the whole
      # picture. Gradle executes dependency and plugin code from the versions
      # this run just picked — this is the untrusted window.
      - name: Run the checks
        if: steps.changed.outputs.changed == 'true'
        id: checks
        env:
          CHECKS: ${{ inputs.checks }}
        run: |
          set +e
          failed=0
          : > checks.md
          while IFS= read -r cmd; do
            [ -z "$cmd" ] && continue
            echo "::group::$cmd"
            # A subshell, so a check that changes shell state — `cd app &&
            # ./gradlew test`, an export — cannot move where the loop writes
            # checks.md or where the next check runs. The runner command
            # files are pointed at /dev/null inside it: they are the channel
            # a planted process would use to forge THIS step's own outputs
            # (a fingerprint written into $GITHUB_OUTPUT from inside a
            # check). Code running as this user can still hunt the real
            # files down on disk — same-machine isolation is not claimed,
            # and the design already reads check results as evidence, not
            # proof — but the casual channel costs one line to close.
            # Stdin redirected too: the loop's own herestring IS the
            # subshell's inherited stdin, so a check that reads stdin
            # (./gradlew's daemon client forwards and drains it; so does
            # anything piping through cat) would otherwise DRAIN the
            # remaining commands — the loop ends early and every unrun
            # check silently never reports, a false pass shaped exactly
            # like the ones this file exists to refuse.
            ( export GITHUB_OUTPUT=/dev/null GITHUB_ENV=/dev/null \
                GITHUB_PATH=/dev/null GITHUB_STEP_SUMMARY=/dev/null \
                GITHUB_STATE=/dev/null
              eval "$cmd" ) < /dev/null
            rc=$?
            echo '::endgroup::'
            if [ $rc -eq 0 ]; then
              echo "- ✅ \`$cmd\`" >> checks.md
            else
              echo "- ❌ \`$cmd\` (exit $rc)" >> checks.md
              failed=1
            fi
          done <<< "$CHECKS"
          # FAIL CLOSED from here: the writes below are the authoritative
          # record, and a record that cannot be written (a check made the
          # file read-only, say) must go red rather than let a forged or
          # missing verdict decide the title and the arming.
          set -e
          cat checks.md >> "$GITHUB_STEP_SUMMARY"
          # checks.md just got its real, final content — fingerprint it
          # now, the same way catalog_sha was captured before any
          # dependency code ran. The checks above ran dependency code in
          # THIS job; a build script's background process could still be
          # alive and rewrite the file on disk before upload-artifact reads
          # it. A step OUTPUT is out of that process's reach (it lives in
          # the runner's control plane, not the filesystem), and publish —
          # a fresh runner that executes no dependency code — verifies the
          # downloaded copy against it.
          echo "checks_sha=$(sha256sum -- checks.md | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"
          # No pass/fail boolean written as this step's own output —
          # publish derives that itself, from checks.md's own (fingerprint-
          # verified) content, rather than trusting a second, independently-
          # forgeable value from this same untrusted step. The sibling
          # npm-update closed this exact gap first; see its AGENTS.md
          # "Trust model". rust-update carries the same fix.
          # Gradle build scripts and plugins ran above and $GITHUB_PATH /
          # $GITHUB_ENV apply to LATER steps, so a planted binary could
          # otherwise shadow `git` and `sha256sum` for the verification
          # below. Emptying them means the runner applies nothing.
          : > "$GITHUB_PATH"
          : > "$GITHUB_ENV"

      # Derived files are the batch's own product: a bundled-license
      # inventory rebuilt from the new dependency set belongs IN the batch,
      # not restored out of it — a consumer whose CI fails on inventory
      # drift would otherwise need a human regeneration push after every
      # benign bump. The declared list is the boundary: these commands run
      # unreviewed dependency and plugin code, so only files named in
      # regenerated-files may change here; anything else still stops the
      # run at the tree check. Each declared file is fingerprinted as a
      # step output the moment the commands finish — like the catalog, so
      # the review checks that run next, and anything else in this
      # untrusted window, can no longer alter what the publish job vouches
      # for.
      - name: Regenerate derived files
        if: steps.changed.outputs.changed == 'true' && inputs.regenerate != ''
        id: regen
        env:
          REGENERATE: ${{ inputs.regenerate }}
          REGENERATED_FILES: ${{ inputs.regenerated-files }}
          CATALOG: ${{ inputs.catalog }}
          GIT_LITERAL_PATHSPECS: '1'
        run: |
          shopt -s lastpipe
          set -euo pipefail
          # The declaration is load-bearing — it is the commit allowlist —
          # so an empty one is a configuration error, not a default.
          files=()
          while IFS= read -r f; do
            [ -z "$f" ] && continue
            files+=("$f")
          done <<< "$REGENERATED_FILES"
          if [ "${#files[@]}" -eq 0 ]; then
            echo '::error::regenerate is set but regenerated-files is empty — declare the files the commands rebuild.'
            exit 1
          fi
          # A duplicate survives the tree check (both entries name the same
          # real change) but not the publish job's restore: it extracts the
          # tar'd artifact once per declared entry, removing each source
          # after copying it out, so the second occurrence of the same path
          # finds nothing left to copy and the run aborts.
          dupes=$(printf '%s\n' "${files[@]}" | sort | uniq -d)
          if [ -n "$dupes" ]; then
            echo "::error::regenerated-files lists the same path more than once:"
            echo "$dupes"
            exit 1
          fi
          for f in "${files[@]}"; do
            # Canonicalized the same way the catalog is: every later
            # comparison against this path — the tree check's allowlist,
            # the fingerprint lookups, the adoption diff — is a plain string
            # match, so a noncanonical spelling like `./licenses.txt` would
            # be declared and fingerprinted here but never match the
            # canonical path `git status` reports later, aborting every
            # batch that actually changes the file. Rewritten in place so
            # every reference from here on uses the one true spelling.
            canon=$(realpath -m --relative-to="$PWD" -- "$f")
            if [ "$canon" != "$f" ]; then
              echo "::error::regenerated-files entry '$f' is not in canonical repo-relative form; use '$canon'."
              exit 1
            fi
            # Scratch names would let a regeneration overwrite the run's
            # own reports and publish command-controlled content under the
            # workflow's name; the catalog is the engine's alone.
            case "$f" in
              report.md|checks.md|deps-stat.txt|review.md|body.md|regen-stat.txt|handoff-catalog.toml|handoff-regenerated.tar)
                echo "::error::regenerated-files must not name the run's own scratch file: $f"
                exit 1 ;;
              .dependency-update|.dependency-update/*)
                # The publish job's artifact staging directory: it copies a
                # verified regenerated file OUT of here by this same name,
                # then `rm -rf`s the whole directory before the commit is
                # staged. A tracked path under this prefix would get
                # written here, deleted by that cleanup, and then
                # committed as a deletion — the exact bytes just verified,
                # discarded by the job that exists to publish them.
                echo "::error::regenerated-files must not name a path under .dependency-update/ — that is the publish job's own staging directory: $f"
                exit 1 ;;
              .gradle-update|.gradle-update/*)
                # Same collision, different job: the update job's "Fetch the
                # validator" step checks the validator repo out here, then
                # `rm -rf`s it once the checks finish — after a regenerated
                # file restored under this prefix would already be deleted.
                echo "::error::regenerated-files must not name a path under .gradle-update/ — that is the validator checkout's own directory: $f"
                exit 1 ;;
            esac
            if [ "$f" = "$CATALOG" ]; then
              echo '::error::regenerated-files must not name the catalog — the engine owns it.'
              exit 1
            fi
            # Tracked files only: the publish job restores each one over a
            # checkout of the base, so a path with no base state has
            # nothing to be a regeneration OF — commit it by hand first.
            git ls-files --error-unmatch -- "$f" >/dev/null 2>&1 || {
              echo "::error::regenerated-files names a file git does not track: $f"
              exit 1
            }
          done
          # Before any regenerate command runs: the checks above already ran
          # unreviewed dependency and plugin code, same as here, and could
          # have left the tree dirty. Two things go wrong without this gate.
          # A regenerate command that resets its own working state first (a
          # "clean build" doing `git checkout -- .` or `git clean`, an
          # ordinary thing to write) would silently erase that evidence
          # before the tree check at the end of this job ever sees it. And a
          # declared file the checks modified — left untouched by a no-op or
          # incremental regeneration — would be fingerprinted and committed
          # as though the regenerate commands produced it, when nothing
          # here actually vouches for it. Same shape as the pre-check the
          # review-checks step below runs, exempting only the catalog and
          # the reports already written by earlier steps — not the declared
          # files, since regeneration has not run yet to earn that.
          # git status runs as its own bare command into a temp file, not
          # piped straight into a chain ending `|| true`: a blanket
          # `|| true` on a longer pipe swallows a genuine git-status
          # failure (a corrupted .git — the Gradle build that already ran
          # above could induce one) exactly as readily as it swallows
          # grep's ordinary no-matches exit 1, so a corrupted git state
          # would read as "nothing dirty" and let this gate silently pass
          # instead of catching exactly what it exists to catch. Each
          # NUL-terminated record is read whole with `read -r -d ''`, not
          # joined into a `tr '\0' '\n'` stream and re-split with sed — a
          # planted filename carrying an embedded newline would otherwise
          # masquerade as two lines, one of which could satisfy an
          # allowlist and hide the other. And membership is a `case`/loop
          # comparison, not grep: grep is line-oriented, so testing one
          # path value that itself contains an embedded newline would
          # still split it and could match on the fragment after the
          # break.
          allow=("$CATALOG" report.md checks.md deps-stat.txt)
          # A pipe, not a scratch file: no name on disk for anything
          # watching $RUNNER_TEMP (a daemonized process a plugin left
          # running from the checks above) to discover and swap the
          # content of between this git status and the read loop opening
          # it — an earlier mktemp version here closed the PREDICTABLE-name
          # version of that attack, but not this one, since a temp file is
          # still reopened by name for reading a moment after it's written
          # (Codex, on review of the mktemp fix in mikelward/rust-update).
          # `shopt -s lastpipe` keeps the loop below in THIS shell rather
          # than a subshell, so $pretree set inside it survives past the
          # pipe — verified directly (`shopt -s lastpipe; printf 'a\nb\n' |
          # while read -r l; do out="$out$l,"; done; echo "$out"` prints
          # "a,b,", not empty). `pipefail` still catches a genuine git
          # status failure the same way the temp-file form's bare command
          # did — verified directly (`shopt -s lastpipe; set -eo pipefail;
          # false | while read l; do :; done; echo unreached` never reaches
          # "unreached"). `if`/`fi`, not `[ ... ] &&`, records a match: a
          # `&&` whose left side is false — every ordinary run's own
          # allowlisted catalog/report changes, the common case — makes
          # that whole statement, and so the loop's own exit status, and so
          # the pipeline under pipefail, nonzero even though nothing is
          # wrong (Codex, on review of the same construct in
          # mikelward/rust-update: verified directly, `shopt -s lastpipe;
          # set -eo pipefail; printf 'a\0' | while IFS= read -r -d '' e; do
          # [ 0 -eq 1 ] && x=1; done; echo unreached` never reaches
          # "unreached"). An `if` with no `else` always exits 0 when its
          # condition is false.
          pretree=""
          git status --porcelain -z --untracked-files=all --no-renames | while IFS= read -r -d '' entry; do
            path=${entry:3}
            keep=1
            for a in "${allow[@]}"; do
              if [ "$path" = "$a" ]; then keep=0; break; fi
            done
            if [ "$keep" -eq 1 ]; then
              pretree="$pretree$path"$'\n'
            fi
          done
          pretree=${pretree%$'\n'}
          plantedallow=(report.md checks.md deps-stat.txt)
          preplanted=""
          git status --porcelain -z --ignored --no-renames | while IFS= read -r -d '' entry; do
            case "$entry" in
              '!! '*) path=${entry:3} ;;
              *) continue ;;
            esac
            case "$path" in
              .gradle/*|*/.gradle/*|.kotlin/*|*/.kotlin/*|local.properties|build/*|*/build/*) continue ;;
            esac
            keep=1
            for a in "${plantedallow[@]}"; do
              if [ "$path" = "$a" ]; then keep=0; break; fi
            done
            if [ "$keep" -eq 1 ]; then
              preplanted="$preplanted$path"$'\n'
            fi
          done
          preplanted=${preplanted%$'\n'}
          if [ -n "$pretree" ] || [ -n "$preplanted" ]; then
            echo '::error::The checks changed the tree outside the version catalog before regeneration ran. Not opening a PR — inspect these first:'
            if [ -n "$pretree" ]; then echo "$pretree"; fi
            if [ -n "$preplanted" ]; then echo "$preplanted"; fi
            exit 1
          fi
          # Fingerprinted before the regenerate commands run, verified after:
          # report.md and deps-stat.txt already exist by now (written by
          # earlier steps) and checks.md is complete, but nothing before this
          # point stops a `regenerate` command — arbitrary Gradle, same as
          # the checks — from writing to them too. Without this, that edit
          # would ride straight through: the tree check later allowlists
          # these three names unconditionally, so a regeneration-authored
          # report would publish as the workflow's own.
          reports_sha_before=$(sha256sum report.md checks.md deps-stat.txt)
          # Same containment as the checks, stdin redirect included — a
          # regenerate command that reads stdin would otherwise drain the
          # herestring feeding this loop and silently skip the commands
          # after it, committing stale derived files. A failure is a STOP,
          # not a flag: a batch whose derived files could not be rebuilt
          # would publish stale ones, which is the silent drift this step
          # exists to end.
          while IFS= read -r cmd; do
            [ -z "$cmd" ] && continue
            echo "::group::$cmd"
            ( export GITHUB_OUTPUT=/dev/null GITHUB_ENV=/dev/null \
                GITHUB_PATH=/dev/null GITHUB_STEP_SUMMARY=/dev/null \
                GITHUB_STATE=/dev/null
              eval "$cmd" ) < /dev/null
            echo '::endgroup::'
          done <<< "$REGENERATE"
          if [ "$(sha256sum report.md checks.md deps-stat.txt)" != "$reports_sha_before" ]; then
            echo '::error::a regenerate command modified report.md, checks.md, or deps-stat.txt — fix the regenerate input; these files belong to the workflow, not to a regeneration command.'
            exit 1
          fi
          # sha256sum's own "hash  path" lines, so the publish job can
          # verify the whole set with one `sha256sum -c`.
          {
            echo 'files_sha<<REGEN_SHA_EOF'
            sha256sum -- "${files[@]}"
            echo 'REGEN_SHA_EOF'
          } >> "$GITHUB_OUTPUT"
          # A second, deliberately NOT-sha256sum-formatted copy for the
          # rerun-adoption loop's per-path lookup below: a filename holding
          # a backslash or newline makes sha256sum itself switch to an
          # escaped line (a leading `\`, `\\`/`\n` inside the name) — fine
          # for `sha256sum -c`, which decodes its own format, but a hand
          # lookup keyed on the raw path would never match the escaped
          # spelling and silently refuse an adoptable branch forever. The
          # hash is always exactly 64 hex characters, so splitting on the
          # first space is unambiguous without any escaping at all.
          {
            echo 'files_hash<<REGEN_HASH_EOF'
            for f in "${files[@]}"; do
              # A leading `\` marks the escaped-filename form; strip it
              # before taking the hash, which is everything up to the
              # first space either way.
              line=$(sha256sum -- "$f")
              line=${line#\\}
              # printf, not echo: `sh`'s echo, unlike bash's default, does
              # interpret backslash sequences in a filename like `\n` —
              # printf never does, so the line stays the raw path.
              printf '%s %s\n' "${line%% *}" "$f"
            done
            echo 'REGEN_HASH_EOF'
          } >> "$GITHUB_OUTPUT"
          # A regeneration command can flip a declared file's executable bit
          # without touching a byte of content — a chmod, not a rewrite —
          # and content-only fingerprints have nothing to say about it.
          # git's own two-state mode (100644 / 100755, the only distinction
          # it stores for a regular file) is what a later `git add` will
          # actually record, so that is what gets fingerprinted and later
          # verified, not raw filesystem permission bits.
          {
            echo 'files_mode<<REGEN_MODE_EOF'
            for f in "${files[@]}"; do
              if [ -x "$f" ]; then echo "100755 $f"; else echo "100644 $f"; fi
            done
            echo 'REGEN_MODE_EOF'
          } >> "$GITHUB_OUTPUT"
          git diff --stat -- "${files[@]}" >> "$GITHUB_STEP_SUMMARY"
          # Plugin code ran above; discard anything it queued for later
          # steps, same as every step in this window.
          : > "$GITHUB_PATH"
          : > "$GITHUB_ENV"

      # Review checks decide whether a HUMAN looks before the batch lands —
      # a nonzero exit flags the PR and holds auto-merge back, it does not
      # fail the batch. They run in the same untrusted window and under the
      # same containment as the checks. A review check may regenerate a
      # tracked file to compare it (the license-drift case); what it changed
      # is captured into the report as the thing the human reviews, then
      # restored — after each command, so no later command can erase the
      # evidence — and the tree check below still holds: nothing a review
      # check produced can ride the artifact. Restoring is only sound when
      # every tracked change is the review checks' own, so a tree the
      # ordinary checks already dirtied is refused up front — the same stop
      # the tree check would have delivered. The catalog itself is never
      # restored here — a review check that touches it fails the fingerprint
      # check below, as anything else would.
      - name: Run the review checks
        if: steps.changed.outputs.changed == 'true' && inputs.review-checks != ''
        id: review
        env:
          REVIEW_CHECKS: ${{ inputs.review-checks }}
          CATALOG: ${{ inputs.catalog }}
          REGENERATED_FILES: ${{ inputs.regenerated-files }}
          # Empty unless the regenerate step actually ran and fingerprinted
          # the declared files — the exemptions below key off THAT, so a
          # declared-but-not-regenerated file is still treated as any other
          # tree change.
          REGEN_SHA: ${{ steps.regen.outputs.files_sha }}
          GIT_LITERAL_PATHSPECS: '1'
        run: |
          shopt -s lastpipe
          set -euo pipefail
          # The restore below attributes every tracked modification to the
          # review checks — which is only true if the tree was clean (bar
          # the catalog) when they started. A tracked change the ORDINARY
          # checks left would otherwise be restored away here, masking
          # exactly what the tree check exists to stop. So that condition
          # is enforced now, with the tree check's own answer: refuse.
          # The same view the tree check uses — index, worktree, AND
          # untracked files — because every shape matters here: a change
          # the checks staged, left in the worktree, or dropped as a new
          # untracked file could otherwise be laundered through the review
          # step's restore before the tree check looks. (--no-renames so a
          # rename never emits the two-field porcelain record this parsing
          # does not read.) The run's own scratch files are the allowlist,
          # as below.
          # Regenerated files join the exemptions the way the catalog
          # does: legitimately changed before this step, never restored by
          # it, and guarded by their own fingerprints below — a review
          # check that touches one fails that check, as anything else
          # would. Keyed off REGEN_SHA, so the exemption exists only when
          # the regenerate step actually ran and fingerprinted them.
          exempt=("$CATALOG" checks.md deps-stat.txt report.md)
          if [ -n "$REGEN_SHA" ]; then
            while IFS= read -r f; do
              [ -z "$f" ] && continue
              exempt+=("$f")
            done <<< "$REGENERATED_FILES"
          fi
          # NUL-safe and status-failure-safe the same way the regenerate
          # step's pretree check above is, and for the same reasons — see
          # that step's comment.
          treestate() {
            local status_z entry path keep a out list_out
            # $1 is required, not defaulted: every caller needs the
            # NUL-delimited list as the source of truth for "did anything
            # change" — the human-readable string this function also
            # returns loses that property across the callers' own `$(...)`
            # (command substitution strips ALL trailing newlines, so an
            # all-newline path collapses it to empty; see the two callers'
            # own comments).
            list_out="$1"
            out=""
            # Kept NUL-delimited here too, not just newline-joined for
            # display: capture_and_restore() below does per-file `git
            # diff`/`reset`/`checkout`/`rm` on whatever this returns, and a
            # review command that leaves a legitimately changed file with an
            # embedded newline in its name would otherwise split into two
            # fragments there, same failure mode this whole fix exists to
            # close — just one step further downstream.
            : > "$list_out"
            # A pipe, not a scratch file: this step already sets
            # `shopt -s lastpipe`, verified to also take effect for a pipe
            # like this one nested inside a function invoked through
            # command substitution — verified directly (`shopt -s lastpipe;
            # f() { local out; out=""; printf 'a\0b\0' | while IFS= read -r
            # -d '' e; do out="$out$e,"; done; printf '%s' "$out"; };
            # x=$(f); echo "$x"` prints "a,b,").
            #
            # `|| return`, not a bare statement — and load-bearing on the
            # PIPE itself, not just on treestate()'s own final line: this
            # function's only callers are `pre=$(treestate "$pre_z")` and
            # `changed=$(treestate "$treestate_list")`, and bash does not
            # propagate a failure from a non-final statement inside a
            # command substitution's function body through `set -e` on its
            # own, whether that statement is a simple command or (still
            # true with `pipefail`) a whole failing pipeline — verified
            # directly both ways (`f() { false; echo after; }; x=$(f)` and
            # `f() { false | while read l; do :; done; echo after; };
            # x=$(f)` under `set -e` each still reach "after" and exit 0,
            # but adding `|| return` to either failing statement stops
            # them). An explicit `return` on failure is what makes the
            # assignment itself see the nonzero status and trip errexit in
            # the caller.
            git status --porcelain -z --untracked-files=all --no-renames | while IFS= read -r -d '' entry; do
              path=${entry:3}
              keep=1
              for a in "${exempt[@]}"; do
                if [ "$path" = "$a" ]; then keep=0; break; fi
              done
              if [ "$keep" -eq 1 ]; then
                out="$out$path"$'\n'
                printf '%s\0' "$path" >> "$list_out"
              fi
            done || return
            printf '%s' "${out%$'\n'}"
          }
          # review.md is NOT exempt at this gate: this step writes its
          # report outside the workspace until the loop is done, so a
          # workspace review.md here was left by an ordinary check — an
          # unexpected file like any other. Refuse.
          # IGNORED paths get the same pre-pass as the final tree check
          # (same invocation, same allowlist): a review command could
          # otherwise remove an ignored file the ordinary checks planted,
          # erasing the evidence before that guard ever looks. The reports
          # the run has already written are exempt by exact name — a
          # consumer's .gitignore matching one reclassifies the workflow's
          # own file as ignored — but review.md is NOT: nothing
          # workflow-owned has written it yet.
          # pre_z is the source of truth for "did anything change", not
          # $pre: command substitution strips ALL trailing newlines, not
          # just the one the function's own `${out%$'\n'}` already trimmed,
          # so a path made ENTIRELY of newline characters (a legal, if
          # bizarre, Linux filename) would collapse $pre to empty even
          # though treestate() found it — verified directly
          # (`f() { printf '%s' "$(printf '\n\n')"; }; x=$(f); echo ${#x}`
          # prints 0). pre_z never crosses that boundary: it is what
          # treestate() itself wrote to disk, byte for byte.
          pre_z=$(mktemp "$RUNNER_TEMP/gradle-update-review-pretree-list-XXXXXX.nul")
          pre=$(treestate "$pre_z")
          plantedallow=(report.md checks.md deps-stat.txt)
          preplanted=""
          git status --porcelain -z --ignored --no-renames | while IFS= read -r -d '' entry; do
            case "$entry" in
              '!! '*) path=${entry:3} ;;
              *) continue ;;
            esac
            case "$path" in
              .gradle/*|*/.gradle/*|.kotlin/*|*/.kotlin/*|local.properties|build/*|*/build/*) continue ;;
            esac
            keep=1
            for a in "${plantedallow[@]}"; do
              if [ "$path" = "$a" ]; then keep=0; break; fi
            done
            if [ "$keep" -eq 1 ]; then
              preplanted="$preplanted$path"$'\n'
            fi
          done
          preplanted=${preplanted%$'\n'}
          if [ -s "$pre_z" ] || [ -n "$preplanted" ]; then
            echo '::error::The checks changed the tree outside the version catalog. Not opening a PR — inspect these first:'
            if [ -n "$pre" ]; then echo "$pre"; fi
            if [ -n "$preplanted" ]; then echo "$preplanted"; fi
            exit 1
          fi
          rm -f -- "$pre_z"
          # The report accumulates OUTSIDE the workspace and is copied in
          # only after the last command: review commands run in the
          # workspace, so one that cleans untracked files (`git clean`,
          # say) could otherwise silently delete the evidence earlier
          # commands left. A workspace review.md appearing DURING the loop
          # is therefore a command's own doing — captured and restored
          # like any other file.
          REVIEW_TMP="$RUNNER_TEMP/review.md"
          : > "$REVIEW_TMP"
          # The verdict record: one canonical line per review command,
          # never capped — the flag publish derives must not depend on
          # what the budget-capped report had room to print.
          : > "$RUNNER_TEMP/review-verdicts"
          omittedcmds=0
          # Two caps: 20,000 bytes per file AND a whole-report budget,
          # because the PR body has a hard 65,536-character limit and a
          # body that exceeds it fails to publish at all — which would
          # hide the review evidence exactly when it matters. The budget
          # is derived from what the REST of the body already spends —
          # report.md, checks.md and deps-stat.txt all exist by now, plus
          # 4,000 for the publish job's boilerplate — and never exceeds
          # 40,000. Once it is spent, the remainder collapses to ONE
          # closing line — a per-file notice each would itself grow
          # without bound. The budget spans all commands: it bounds the
          # report, not any one command's share of it.
          # The reports written earlier in the run are finished products
          # from here on; fingerprinted now so a review command that
          # rewrites one (a broad formatter, say) is caught below rather
          # than publishing command-controlled content under the
          # workflow's name — or growing a file past the budget derived
          # from its size.
          reports_sha=$(sha256sum report.md checks.md deps-stat.txt)
          other=$(( $(wc -c < report.md) + $(wc -c < checks.md) + $(wc -c < deps-stat.txt) ))
          budget=$(( 65536 - 4000 - other ))
          if [ "$budget" -gt 40000 ]; then budget=40000; fi
          if [ "$budget" -lt 0 ]; then budget=0; fi
          omitted=0
          # Capture $1's tree changes into review.md, then restore them —
          # ONE COMMAND AT A TIME, so every command starts from the tree
          # the ordinary checks left and a later command can neither
          # overwrite nor quietly undo the evidence an earlier one left
          # (a cleanup command restoring the regenerated license list
          # would otherwise leave a flagged PR with nothing to review).
          # The status view covers every shape a command can leave —
          # staged, worktree, staged-then-reverted (index differs while
          # the worktree matches HEAD), or a new untracked file; git diff
          # HEAD alone misses the last two. Nothing is exempt: the report
          # lives outside the workspace, so even a review.md a command
          # drops is that command's own change, captured and restored.
          capture_and_restore() {
            # mktemp'd before calling treestate, not a fixed name — the
            # same predictable-path/symlink concern as every other scratch
            # file in this fix, and this one doubles as the restore loops'
            # own input below, so a corrupted or /dev/null'd list here
            # would mean commands the review checks ran are never restored.
            treestate_list=$(mktemp "$RUNNER_TEMP/gradle-update-treestate-list-XXXXXX.nul")
            changed=$(treestate "$treestate_list")
            # -s "$treestate_list", not -z "$changed": command substitution
            # strips ALL trailing newlines, not just the one treestate()
            # itself trims, so a path made entirely of newline characters
            # would collapse $changed to empty even though the list file
            # holds a real NUL-terminated record — verified directly
            # (`f() { printf '%s' "$(printf '\n\n')"; }; x=$(f); echo
            # ${#x}` prints 0). The list file never crosses that boundary.
            if [ ! -s "$treestate_list" ]; then
              rm -f -- "$treestate_list"
              return 0
            fi
            # The heading carries the command text — check-controlled
            # length — so it is charged with its fences as one unit before
            # anything is emitted; once it no longer fits, this command's
            # whole report collapses into the closing notice. The restore
            # below runs regardless — only reporting is capped.
            heading=$'\n'"What \`$1\` changed (restored after capture):"$'\n\n''```diff'
            budget=$((budget - $(printf '%s\n' "$heading" | wc -c) - 4))
            if [ "$budget" -ge 0 ]; then
              printf '%s\n' "$heading" >> "$REVIEW_TMP"
              reported=1
            else
              reported=0
            fi
            while IFS= read -r -d '' f; do
              if [ "$reported" -eq 0 ] || [ "$budget" -le 0 ]; then
                omitted=$((omitted + 1))
                # No room in the report, but the evidence must still live
                # somewhere a reviewer can reach: the step log takes the
                # full diff — logs have no body limit.
                echo "::group::omitted from the report — diff of $f"
                git diff --cached --no-renames -- "$f"
                git diff --no-renames -- "$f"
                echo '::endgroup::'
                continue
              fi
              # Both halves, because either can be empty while the other
              # carries the change: index vs HEAD (--cached) and worktree
              # vs index. Staged in a file OUTSIDE the workspace and
              # truncated from there: `git diff | head -c` would take
              # SIGPIPE on a large diff under pipefail and kill the step
              # mid-restore.
              {
                git diff --cached --no-renames -- "$f"
                git diff --no-renames -- "$f"
              } > "$RUNNER_TEMP/review-diff"
              size=$(wc -c < "$RUNNER_TEMP/review-diff")
              # Every line this loop emits is charged against the budget,
              # notices included: their lengths carry check-controlled
              # filenames, so uncharged notices would grow the report
              # without bound — the exact hole the budget closes.
              if [ "$size" -eq 0 ]; then
                note="new file added by a review check (content not shown): $f"
                budget=$((budget - $(printf '%s\n' "$note" | wc -c)))
                if [ "$budget" -lt 0 ]; then
                  omitted=$((omitted + 1))
                else
                  printf '%s\n' "$note" >> "$REVIEW_TMP"
                fi
                continue
              fi
              take=$(( size < 20000 ? size : 20000 ))
              if [ "$take" -gt "$budget" ]; then take=$budget; fi
              if [ "$take" -gt 0 ]; then
                head -c "$take" "$RUNNER_TEMP/review-diff" >> "$REVIEW_TMP"
                budget=$((budget - take))
              fi
              if [ "$take" -lt "$size" ]; then
                # Charged BEFORE emitting, like every other line: the
                # notice carries the file's path, and a near-PATH_MAX name
                # could otherwise overshoot the boilerplate reserve. When
                # it does not fit, the closing notice covers it — the
                # file counts as omitted.
                note="… diff of $f truncated at $take of $size bytes (per-file and whole-report caps keep the PR body publishable) — the step log has it in full."
                budget=$((budget - $(printf '\n%s\n' "$note" | wc -c)))
                if [ "$budget" -ge 0 ]; then
                  printf '\n%s\n' "$note" >> "$REVIEW_TMP"
                else
                  omitted=$((omitted + 1))
                fi
                echo "::group::truncated in the report — full diff of $f"
                cat "$RUNNER_TEMP/review-diff"
                echo '::endgroup::'
              fi
            done < "$treestate_list"
            if [ "$reported" -eq 1 ]; then
              echo '```' >> "$REVIEW_TMP"
            fi
            # A path the review checks ADDED (or the add half of a rename)
            # has nothing in HEAD to check out — restoring it means
            # unstaging it and deleting the file, where a modified or
            # deleted path restores from HEAD.
            while IFS= read -r -d '' f; do
              git reset -q -- "$f"
              if git cat-file -e "HEAD:$f" 2>/dev/null; then
                git checkout -q HEAD -- "$f"
              else
                rm -f -- "$f"
              fi
            done < "$treestate_list"
            rm -f -- "$treestate_list"
          }
          # The whole loop runs fail-closed (set -e stays on): only each
          # command's own exit is caught, because a nonzero there is the
          # flag, not an error. The capture and restore are authoritative
          # — a failure in them must go red, not fall through.
          while IFS= read -r cmd; do
            [ -z "$cmd" ] && continue
            echo "::group::$cmd"
            rc=0
            # Captured to a file OUTSIDE the workspace so a flagging
            # command's own words can go into the report below; the log
            # still gets every byte, and a command that cleans the
            # workspace cannot delete this.
            # Stdin from /dev/null for the same reason as the other two
            # command loops: the herestring feeding this loop is otherwise
            # the subshell's stdin, and a command that reads it would
            # silently drain the review checks after it — a dropped
            # flagging command leaves auto-merge armed.
            ( export GITHUB_OUTPUT=/dev/null GITHUB_ENV=/dev/null \
                GITHUB_PATH=/dev/null GITHUB_STEP_SUMMARY=/dev/null \
                GITHUB_STATE=/dev/null
              eval "$cmd" ) < /dev/null > "$RUNNER_TEMP/review-out" 2>&1 || rc=$?
            cat -- "$RUNNER_TEMP/review-out"
            echo '::endgroup::'
            # The verdict line carries the command text too, so it is
            # charged the same way; a line past the cap folds into the
            # closing notice — an UNconditional write here would let a
            # long configured command list grow the body past GitHub's
            # 65,536-character limit and fail the body edit outright,
            # hiding every report at once (Codex review). The FLAG itself
            # never depends on what the report had room to print: every
            # verdict line also lands in the uncapped verdict record
            # below, which crosses to publish as a step output.
            if [ "$rc" -eq 0 ]; then
              line="- ✅ \`$cmd\`"
            else
              line="- 🚩 \`$cmd\` (exit $rc) — human review requested"
            fi
            budget=$((budget - $(printf '%s\n' "$line" | wc -c)))
            if [ "$budget" -ge 0 ]; then
              printf '%s\n' "$line" >> "$REVIEW_TMP"
            else
              omittedcmds=$((omittedcmds + 1))
            fi
            printf '%s\n' "$line" >> "$RUNNER_TEMP/review-verdicts"
            # A flagging command's own output is the REASON a human is
            # being asked to look, but it is NOT safe to copy here: this
            # command ran in the same untrusted window as `regenerate`, and
            # its output was captured to review-out by REDIRECTING the
            # subshell's stdout straight to a file — bytes that never pass
            # through the runner's own log-capture pipeline. GitHub's
            # secret masking scrubs THAT pipeline (which is why the `cat`
            # above shows `***` for a registered secret in the step log),
            # not arbitrary files a step writes; review-out itself is the
            # raw, unmasked bytes. review.md is committed into a public PR
            # body, so copying review-out there would publish anything the
            # command printed — a token it read from the environment,
            # included. Point at the step log instead, which already
            # carries the full output AND the real redaction. Green
            # commands stay quiet: their output is progress noise, and the
            # ✅ line says everything the body needs.
            if [ "$rc" -ne 0 ] && [ -s "$RUNNER_TEMP/review-out" ]; then
              note="What \`$cmd\` reported: see the step log above — its output is not copied into this report, since GitHub only redacts registered secrets in the log, not in files this job writes."
              budget=$((budget - $(printf '%s\n' "$note" | wc -c) - 1))
              if [ "$budget" -ge 0 ]; then
                printf '\n%s\n' "$note" >> "$REVIEW_TMP"
              else
                omitted=$((omitted + 1))
              fi
            fi
            capture_and_restore "$cmd"
          done <<< "$REVIEW_CHECKS"
          rm -f -- "$RUNNER_TEMP/review-diff" "$RUNNER_TEMP/review-out"
          if [ "$omitted" -gt 0 ] || [ "$omittedcmds" -gt 0 ]; then
            {
              echo
              echo "… whole-report cap reached — omitted: $omittedcmds command result(s) and diffs of $omitted changed file(s); the step log carries every omitted diff in full."
            } >> "$REVIEW_TMP"
          fi
          # The run's other reports live in the workspace, where a review
          # command that cleans untracked files could have deleted them.
          # That surfaces later as an opaque artifact failure — name the
          # actual cause here instead.
          for f in report.md checks.md deps-stat.txt; do
            if [ ! -f "$f" ]; then
              echo "::error::a review check deleted the run's own $f — fix the review-checks input; a review command must not touch the workflow's scratch files."
              exit 1
            fi
          done
          if [ "$(sha256sum report.md checks.md deps-stat.txt)" != "$reports_sha" ]; then
            echo "::error::a review check modified the run's own reports — fix the review-checks input; a review command must not touch the workflow's scratch files."
            exit 1
          fi
          # Only now does the report enter the workspace, after the last
          # command has run and been restored.
          cp -- "$REVIEW_TMP" review.md
          cat review.md >> "$GITHUB_STEP_SUMMARY"
          # The verdict record crosses to publish as a step output — the
          # control-plane channel the fingerprints already ride, out of
          # reach of a background process rewriting files before
          # upload-artifact reads them — and publish derives the review
          # flag from IT, never from a boolean this untrusted step
          # reported about its own run and never from review.md, whose
          # verdict lines the body budget may legitimately have capped.
          # review.md itself is informational from here on, like
          # report.md and deps-stat.txt. The delimiter follows the
          # REGEN_SHA_EOF convention above; a configured review command
          # that is literally this delimiter line would corrupt its own
          # record, and the count mismatch in publish then flags the
          # batch rather than passing it.
          {
            echo 'verdicts<<REVIEW_VERDICTS_EOF'
            cat -- "$RUNNER_TEMP/review-verdicts"
            echo 'REVIEW_VERDICTS_EOF'
          } >> "$GITHUB_OUTPUT"
          # Same reasoning as the checks step: review checks ran dependency
          # code, so discard anything they queued for later steps.
          : > "$GITHUB_PATH"
          : > "$GITHUB_ENV"

      # The checks above and the artifact below have to describe the SAME
      # tree. Anything outside the catalog is a stop, not something to strip
      # silently: a dependency that rewrites your source during a build is a
      # decision for a human.
      - name: Verify only the catalog changed
        if: steps.changed.outputs.changed == 'true'
        env:
          CATALOG: ${{ inputs.catalog }}
          CATALOG_SHA: ${{ steps.changed.outputs.catalog_sha }}
          REVIEW_CHECKS: ${{ inputs.review-checks }}
          REGENERATED_FILES: ${{ inputs.regenerated-files }}
          # Empty unless the regenerate step ran and fingerprinted the
          # declared files — every regenerated-file exemption below keys
          # off that, never off the declaration alone.
          REGEN_SHA: ${{ steps.regen.outputs.files_sha }}
          REGEN_MODE: ${{ steps.regen.outputs.files_mode }}
        run: |
          shopt -s lastpipe
          set -euo pipefail
          # `git status`, not `git diff`: diff only sees tracked changes, so
          # an untracked file dropped into the tree by a plugin would sail
          # past it. The allowed extras are files this job writes itself.
          # `grep -Fx`, not a regex: the catalog path is data, and even the
          # default path's dots would otherwise match any character — a
          # planted `gradle/libsXversionsYtoml` must not ride the allowlist,
          # and a metacharacter in a custom path must not break the guard.
          # `-z` so git emits paths raw: its C-quoting of spaces and
          # non-ASCII would otherwise never equal the literal $CATALOG and
          # fail every publication under such a path.
          allow=("$CATALOG" checks.md deps-stat.txt report.md)
          # Regenerated files are the batch's own product when the
          # regenerate step ran — expected to differ, vouched for by the
          # fingerprint check below rather than by this allowlist.
          derived=()
          if [ -n "$REGEN_SHA" ]; then
            while IFS= read -r f; do
              [ -z "$f" ] && continue
              derived+=("$f")
              allow+=("$f")
            done <<< "$REGENERATED_FILES"
          fi
          # review.md is workflow-owned only when review checks ran (that
          # step creates it, after refusing a preexisting one). With review
          # checks disabled nothing here writes it, so one appearing now is
          # a check's doing — exempting it would ship that content into the
          # PR body's Review section.
          if [ -n "$REVIEW_CHECKS" ]; then allow+=(review.md); fi
          # Pipe, not a scratch file, and NUL-safe/status-failure-safe the
          # same way the regenerate step's pretree check is — see that
          # step's comment.
          unexpected=""
          git status --porcelain -z --untracked-files=all --no-renames | while IFS= read -r -d '' entry; do
            path=${entry:3}
            keep=1
            for a in "${allow[@]}"; do
              if [ "$path" = "$a" ]; then keep=0; break; fi
            done
            if [ "$keep" -eq 1 ]; then
              unexpected="$unexpected$path"$'\n'
            fi
          done
          unexpected=${unexpected%$'\n'}
          if [ -n "$unexpected" ]; then
            echo '::error::The batch touched files outside the version catalog. Not opening a PR — inspect these first:'
            echo "$unexpected"
            exit 1
          fi
          # IGNORED paths as a separate pass — `--ignored` alone collapses a
          # fully-ignored directory to one entry, which is what makes looking
          # affordable; combined with --untracked-files=all it would list all
          # of ~/.gradle-scale noise file by file. The allowlist is Gradle's
          # own outputs; anything else ignored is something new in the tree
          # and worth a human look. The run's own scratch files are exempt
          # by exact name: a consumer whose .gitignore happens to match one
          # (a broad `*.md` rule, say) reclassifies the workflow's own
          # report as ignored, and without the exemption every weekly run
          # would abort on a file this job wrote itself. review.md is
          # workflow-owned only when review checks ran, same rule as the
          # untracked pass above — with them disabled, a gitignored
          # review.md a check dropped must still stop the run, or it would
          # ride the artifact into the PR body through the one pass that
          # no longer sees it.
          plantedallow=(report.md checks.md deps-stat.txt)
          if [ -n "$REVIEW_CHECKS" ]; then plantedallow+=(review.md); fi
          planted=""
          git status --porcelain -z --ignored --no-renames | while IFS= read -r -d '' entry; do
            case "$entry" in
              '!! '*) path=${entry:3} ;;
              *) continue ;;
            esac
            case "$path" in
              .gradle/*|*/.gradle/*|.kotlin/*|*/.kotlin/*|local.properties|build/*|*/build/*) continue ;;
            esac
            keep=1
            for a in "${plantedallow[@]}"; do
              if [ "$path" = "$a" ]; then keep=0; break; fi
            done
            if [ "$keep" -eq 1 ]; then
              planted="$planted$path"$'\n'
            fi
          done
          planted=${planted%$'\n'}
          if [ -n "$planted" ]; then
            echo '::error::The batch left unexpected IGNORED files in the tree. Not opening a PR — inspect these first:'
            echo "$planted"
            exit 1
          fi
          # Nothing may have touched the catalog since the engine rewrote it.
          if [ "$(sha256sum -- "$CATALOG" | cut -d' ' -f1)" != "$CATALOG_SHA" ]; then
            echo '::error::The catalog changed AFTER the engine resolved it — something in the check run rewrote it. Not opening a PR.'
            exit 1
          fi
          # Nor the regenerated files since their step fingerprinted them —
          # this is what lets the review step leave them unrestored: any
          # later hand on one of them stops the run here.
          if [ -n "$REGEN_SHA" ]; then
            printf '%s\n' "$REGEN_SHA" | sha256sum -c --strict --quiet \
              || { echo '::error::A regenerated file changed AFTER the regenerate step fingerprinted it — something in the review run rewrote it. Not opening a PR.'; exit 1; }
            # A content-only fingerprint has nothing to say about a chmod:
            # the bytes match, and the tree checks above deliberately treat
            # a declared regenerated file as expected-to-differ, so a
            # review check flipping the executable bit alone would
            # otherwise ride straight through both checks. Same fingerprint
            # discipline, one bit of state instead of a hash.
            while IFS= read -r mode_line; do
              [ -z "$mode_line" ] && continue
              # Split only the fixed "MODE " prefix — `read` would trim
              # leading/trailing spaces from a filename, silently missing a
              # mismatch for a declared path that has any.
              want_mode=${mode_line%% *}
              f=${mode_line#* }
              if [ -x "$f" ]; then have_mode=100755; else have_mode=100644; fi
              if [ "$have_mode" != "$want_mode" ]; then
                echo "::error::A regenerated file's executable bit changed AFTER the regenerate step fingerprinted it ($f: expected $want_mode, now $have_mode) — something in the review run rewrote it. Not opening a PR."
                exit 1
              fi
            done <<< "$REGEN_MODE"
          fi
          # The artifact names review.md unconditionally; a run with no
          # review checks configured still ships an empty one.
          [ -f review.md ] || : > review.md
          # The regenerated files travel as ONE tar under a fixed literal
          # name, for the same reason the catalog travels as
          # handoff-catalog.toml: upload-artifact reads its path list as
          # glob patterns, and consumer paths are data. NUL-delimited
          # verbatim names so no filename shape can smuggle options or
          # split into two. Always created — empty when the feature is
          # off — so the artifact's fixed path list never goes missing.
          if [ "${#derived[@]}" -gt 0 ]; then
            printf '%s\0' "${derived[@]}" | tar -cf handoff-regenerated.tar --null --verbatim-files-from -T -
          else
            tar -cf handoff-regenerated.tar -T /dev/null
          fi
          # Stage the catalog for the artifact under a fixed literal name:
          # upload-artifact reads its path list as GLOB PATTERNS, so a
          # metacharacter in a custom catalog path ([, ?) could silently
          # drop the catalog while the other three files keep
          # if-no-files-found happy — the publish job would then fail its
          # fingerprint check on every run. Copied AFTER the tree checks so
          # the copy itself never trips them.
          cp -- "$CATALOG" handoff-catalog.toml

      - name: Hand off the dependency diff
        if: steps.changed.outputs.changed == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          # Four fixed literal names — nothing here came from an input, so
          # the action's glob reading cannot misinterpret any of them, and a
          # catalog under a hidden directory no longer needs special-casing.
          name: dependency-update
          path: |
            handoff-catalog.toml
            handoff-regenerated.tar
            checks.md
            review.md
            deps-stat.txt
            report.md
          retention-days: 1
          if-no-files-found: error

  # Publishes. Runs on a fresh runner, installs nothing, and executes no
  # dependency code — so the write token is never on a machine where an
  # unreviewed dependency has run.
  publish:
    needs: update
    if: needs.update.outputs.changed == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # The environment whose secrets this job may read — see the
    # `environment` input. Only this job declares it, so an environment-
    # scoped credential is never delivered to the update job's runner.
    environment: ${{ inputs.environment }}
    permissions:
      # This block scopes only the job's own auto-generated GITHUB_TOKEN.
      # When a consumer supplies app-id/app-private-key below, the token
      # this job actually uses is a separate GitHub App installation token,
      # and its effective access comes from the App's OWN permissions (set
      # at installation — see docs/GITHUB_APP.md), not from this block. Kept
      # in step with what GITHUB_TOKEN would need regardless, since a
      # consumer that omits the secrets still runs on it.
      contents: write
      pull-requests: write
      # Dispatching the consumer's CI on the pushed branch is a write to the
      # Actions API — granted HERE and not to the update job, so dependency
      # code can never start a workflow.
      actions: write
    # Read once into env, not referenced as secrets.app-id / secrets.
    # app-private-key directly in the if: conditions below — the secrets
    # context is unavailable there for a reusable workflow's own
    # workflow_call secrets, so an if: naming them outright would just
    # never select the intended branch.
    env:
      # The hub-named secret first — what `secrets: inherit` delivers from
      # the environment above (or, until it is moved there, from the
      # repository) — then the legacy explicitly-passed one.
      APP_ID: ${{ secrets.GRADLE_UPDATE_APP_ID || secrets.app-id }}
      APP_PRIVATE_KEY: ${{ secrets.GRADLE_UPDATE_APP_PRIVATE_KEY || secrets.app-private-key }}
      PAT: ${{ secrets.GRADLE_UPDATE_PAT || secrets.token }}
    steps:
      # A partial credential mints no token either way; refuse it explicitly
      # rather than silently falling back to GITHUB_TOKEN, which would read
      # as "the App is wired up" while actually still hitting the approval
      # gate docs/GITHUB_APP.md describes. Skipped once a token is set: the
      # App path is irrelevant then (PAT takes priority — see the GH_TOKEN
      # resolution below), so a leftover half-removed app-id from a PAT
      # migration must not fail a run it can no longer affect.
      - name: Refuse a half-supplied App credential
        if: env.PAT == '' && (env.APP_ID == '') != (env.APP_PRIVATE_KEY == '')
        run: |
          echo "::error::app-id and app-private-key must both be set, or both left empty — one without the other mints no token."
          exit 1

      # Optional: a consumer that wired up docs/GITHUB_APP.md gets a
      # short-lived installation token here instead of GITHUB_TOKEN, so the
      # pull request this job opens is authored by a real collaborator and
      # never trips the first-time-contributor approval gate on the
      # consumer's own on: pull_request workflows — including this job's
      # own dispatch fallback below, which the App token's identity being a
      # real collaborator makes unnecessary. Not pinned by SHA yet — see
      # TODO.md. Skipped when a token is set: PAT takes priority, so minting
      # an App installation token that GH_TOKEN resolution would then
      # discard unused is a needless credential mint.
      - name: Mint a GitHub App installation token
        id: app-token
        if: env.PAT == '' && env.APP_ID != ''
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ env.APP_ID }}
          private-key: ${{ env.APP_PRIVATE_KEY }}
          # Named explicitly rather than left to inherit the App's whole
          # installation grant — matches docs/GITHUB_APP.md's two
          # permissions exactly, so the minted token can never be wider
          # than what this job actually uses even if the App is later
          # installed with more (zizmor: github-app).
          permission-contents: write
          permission-pull-requests: write

      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # v5.0.1
        with:
          # The exact commit the update job tested, not the branch tip.
          ref: ${{ needs.update.outputs.base }}
          persist-credentials: false

      - uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0  # v5.0.0
        with:
          name: dependency-update
          # Its own directory, so no artifact filename can collide with a
          # consumer's real path — a catalog literally named
          # handoff-catalog.toml at the root would otherwise be the cp's own
          # destination and fail the publish forever.
          path: .dependency-update

      # The artifact traveled through a machine that ran dependency code;
      # check it against the fingerprint taken before any of that ran. The
      # fingerprint came across as a job output, which lives in the runner's
      # control plane rather than on that machine's disk. The catalog rode
      # the artifact under its fixed handoff name (upload-artifact globs its
      # path list, so the real path never goes near it) and is restored to
      # its true path here — the catalog is a tracked file, so its directory
      # already exists in the checkout. The catalog moves LAST, so even a
      # catalog named like one of the report files ends up holding the
      # verified catalog bytes, which the fingerprint then vouches for.
      - name: Check the artifact against the pre-check fingerprint
        env:
          CATALOG: ${{ inputs.catalog }}
          # Update-job outputs enter every run block in this job through env,
          # never inline: `${{ }}` expands into the script BEFORE bash runs,
          # so an output forged on the update runner (the file-command
          # channel its checks step documents as closable only casually)
          # would otherwise become code under this job's write token. As env
          # it is data, and a forged value is bounded to a failed comparison.
          CATALOG_SHA: ${{ needs.update.outputs.catalog_sha }}
          CHECKS_SHA: ${{ needs.update.outputs.checks_sha }}
          REGENERATED_FILES: ${{ inputs.regenerated-files }}
          REGEN_SHA: ${{ needs.update.outputs.regen_sha }}
          REGEN_MODE: ${{ needs.update.outputs.regen_mode }}
          # The diffstat below names consumer paths — data, not patterns.
          GIT_LITERAL_PATHSPECS: '1'
        run: |
          set -euo pipefail
          mv -- .dependency-update/checks.md .dependency-update/review.md .dependency-update/deps-stat.txt .dependency-update/report.md .
          # checks.md gates the PR title and auto-merge (the verdict step
          # below derives the check verdict from its content), so it gets
          # the same artifact-vs-control-plane check as the catalog.
          # deps-stat.txt, report.md, and review.md stay unverified on
          # purpose: purely informational, they gate nothing — the review
          # FLAG crosses as the review step's verdict-record output, a
          # control-plane value that needs no artifact verification.
          [ "$(sha256sum -- checks.md | cut -d' ' -f1)" = "$CHECKS_SHA" ] \
            || { echo '::error::checks.md does not match what the update job recorded.'; exit 1; }
          # The regenerated files, before the catalog so the catalog still
          # moves last. The tar was written on the machine that ran
          # dependency code, so nothing in it is trusted by name: it is
          # unpacked OUTSIDE the workspace, only the files the consumer's
          # own configuration declares are copied out of it — each one a
          # tracked path, restored over the base checkout — anything extra
          # in it is a stop, and the copies must then match the
          # fingerprints taken before any review code ran.
          : > regen-stat.txt
          if [ -n "$REGEN_SHA" ]; then
            mkdir -- "$RUNNER_TEMP/regenerated"
            tar -xf .dependency-update/handoff-regenerated.tar -C "$RUNNER_TEMP/regenerated"
            derived=()
            while IFS= read -r f; do
              [ -z "$f" ] && continue
              derived+=("$f")
              [ -f "$RUNNER_TEMP/regenerated/$f" ] \
                || { echo "::error::The artifact is missing declared regenerated file: $f"; exit 1; }
              # `-L`, not `-f`: a symlink to a regular file passes `-f` (it
              # dereferences), which is exactly the shape that would slip
              # past this check and into the `cp` below. A regenerated file
              # is always meant to be a plain file — never legitimately a
              # symlink — so refuse rather than try to preserve one: `cp`
              # without `-P` follows a SOURCE symlink too, so restoring one
              # would either copy through to whatever it points at inside
              # this extraction directory (silently wrong content) or fail
              # outright if that target doesn't exist there, while the
              # fingerprint and `git add` below both read through the same
              # symlink and would never see the file-type change.
              if [ -L "$RUNNER_TEMP/regenerated/$f" ]; then
                echo "::error::regenerated-files artifact carries a symlink, not a regular file: $f"
                exit 1
              fi
              # Remove the destination before copying: if the base
              # checkout holds $f as a SYMLINK, `cp` without this would
              # follow it and write the verified bytes through to
              # whatever it points at, leaving the symlink itself (and
              # whatever it names) unchanged — the fingerprint check below
              # would then pass by reading through the same symlink, and
              # `git add` would stage no change at all.
              rm -f -- "$f"
              cp -- "$RUNNER_TEMP/regenerated/$f" "$f"
              rm -- "$RUNNER_TEMP/regenerated/$f"
            done <<< "$REGENERATED_FILES"
            leftover=$(find "$RUNNER_TEMP/regenerated" -type f -o -type l | head -n 1)
            if [ -n "$leftover" ]; then
              echo "::error::The regenerated-files artifact carried an undeclared file: $leftover"
              exit 1
            fi
            rm -rf -- "$RUNNER_TEMP/regenerated"
            printf '%s\n' "$REGEN_SHA" | sha256sum -c --strict --quiet \
              || { echo '::error::A regenerated file does not match what the update job fingerprinted.'; exit 1; }
            # Same reasoning as the update job's own re-check: content can
            # match while the executable bit doesn't — a chmod between
            # fingerprinting and here, or a plugin that runs `tar` with its
            # own idea of default modes — and `git add` below would commit
            # whatever bit the restored file happens to carry.
            while IFS= read -r mode_line; do
              [ -z "$mode_line" ] && continue
              # Split only the fixed "MODE " prefix — see the update job's
              # own re-check for why `read`'s whitespace-trimming is unsafe
              # here.
              want_mode=${mode_line%% *}
              f=${mode_line#* }
              if [ -x "$f" ]; then have_mode=100755; else have_mode=100644; fi
              if [ "$have_mode" != "$want_mode" ]; then
                echo "::error::A regenerated file's executable bit does not match what the update job fingerprinted ($f: expected $want_mode, got $have_mode)."
                exit 1
              fi
            done <<< "$REGEN_MODE"
            # The body's Regenerated section, taken NOW while the tree
            # still diffs against the base — after the commit below there
            # is nothing left to diff, and the adopted-branch path never
            # commits at all.
            git diff --stat -- "${derived[@]}" > regen-stat.txt
          fi
          cp -- .dependency-update/handoff-catalog.toml "$CATALOG"
          rm -rf -- .dependency-update
          [ "$(sha256sum -- "$CATALOG" | cut -d' ' -f1)" = "$CATALOG_SHA" ] \
            || { echo '::error::The catalog does not match what the update job resolved.'; exit 1; }

      - name: Fetch the validator
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # v5.0.1
        with:
          repository: mikelward/gradle-update
          # The same pin the update job used — the revision of the workflow
          # that is running, read from the runner's own context rather than
          # from anything the update job reported. The batch is validated
          # under the rules that resolved it, and nothing the untrusted job
          # writes can choose what code runs here.
          ref: ${{ job.workflow_sha }}
          path: .gradle-update
          persist-credentials: false

      # The authoritative check on what this commit contains, and the reason
      # it lives HERE: this runner has executed no dependency code, so
      # `git show HEAD:<catalog>` and the downloaded artifact are both out of
      # the batch's reach. Only in-place, in-major, stable version bumps in
      # [versions] pass — and --verify-upstream re-asks the repositories that
      # each new version exists for every module sharing its key, published
      # outside the cooldown. That last part cannot come from the artifact or
      # the fingerprint: both originated on the machine that later ran the
      # batch's own Gradle code, so code compromised within-major could forge
      # a matching pair naming an hours-old release. Metadata over HTTPS from
      # a clean runner is exactly the work the trust split allows here.
      - name: Re-validate the dependency diff from a clean context
        env:
          CATALOG: ${{ inputs.catalog }}
          COOLDOWN_DAYS: ${{ inputs.cooldown-days }}
          # Both jobs read the SAME inputs: a divergence would make this job
          # reject exactly what the update job produced — fail-closed, but
          # baffling to whoever read the PR body.
          EXTRA_REPOSITORIES: ${{ inputs.extra-repositories }}
          NO_COOLDOWN_FOR: ${{ inputs.no-cooldown-for }}
        run: |
          set -euo pipefail
          node .gradle-update/check-gradle-update.mjs \
            --catalog "$CATALOG" --verify-upstream --cooldown-days "$COOLDOWN_DAYS" \
            --extra-repositories "$EXTRA_REPOSITORIES" \
            --no-cooldown-for "$NO_COOLDOWN_FOR"
          rm -rf .gradle-update

      - name: Stop if the default branch moved while the checks ran
        env:
          GH_TOKEN: ${{ env.PAT || steps.app-token.outputs.token || github.token }}
          # Data, not script text — an apostrophe is legal in a git ref name.
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          # Env, not inline — see the fingerprint step.
          BASE: ${{ needs.update.outputs.base }}
        run: |
          set -euo pipefail
          # Percent-encoded: the argument is a URL path, and characters like
          # `#` are legal in a ref name — docs-lane.sh encodes for the same
          # reason.
          encoded=$(printf %s "$DEFAULT_BRANCH" | jq -sRr @uri)
          current=$(gh api "repos/${{ github.repository }}/commits/$encoded" --jq .sha)
          if [ "$current" != "$BASE" ]; then
            echo "::error::$DEFAULT_BRANCH moved from $BASE to $current while the checks ran, so the verified catalog and the branch it would land on no longer match. Re-run the workflow."
            exit 1
          fi

      # Split from the step below purely on size: a single `run:` block is a
      # YAML scalar node, and GitHub Actions caps any one node at 21,000
      # characters — this step's adopt-or-commit-and-push logic plus the PR
      # composition that used to follow it together cleared that limit,
      # which fails the ENTIRE workflow_call at dispatch time (a parse
      # error, before any job runs) rather than failing one step. The split
      # point is a real seam: this half decides the branch name and either
      # adopts an existing one or commits and pushes a new one; the next
      # half only opens/updates the PR. What crosses the boundary — branch,
      # The update job used to report `passed` and `flagged` booleans about
      # its own checks — produced inside the same untrusted steps that ran
      # dependency code, so a compromised build script's background process
      # could append `passed=true` (or `flagged=false`) to the step's real
      # $GITHUB_OUTPUT after the honest write (later duplicate keys win;
      # the runner captures the file only at step end) and the PR would
      # title itself clean, and arm auto-merge, while checks.md showed ❌
      # lines in the same body. Deriving both verdicts HERE, from the
      # fingerprint-verified files themselves, collapses each pair of
      # independently-forgeable signals to one: the file this job just
      # checked. Not a complete fix — a forger who fakes a file
      # consistently with its own fingerprint inside the step's window
      # still isn't caught — but it matches the posture the sibling
      # npm-update took first and rust-update carries too.
      #
      # The check verdict fails closed: a line that doesn't match the exact
      # format the checks step writes, a check identity the consumer didn't
      # configure, or a configured check recorded a different number of
      # times than it was configured are all treated as failed, never
      # silently passed. The configured lists are trusted ground truth —
      # they come from the consumer's own workflow file via workflow_call,
      # not from anything the update runner produced.
      #
      # The review flag fails closed toward FLAGGED (the direction that
      # holds auto-merge and asks a human), and derives from the review
      # step's VERDICT RECORD output — one canonical line per configured
      # command, never budget-capped, carrying no diff content — rather
      # than from review.md, whose verdict lines the body budget may
      # legitimately have capped and whose captured diffs could resemble
      # verdict lines. Every record line must exactly match a configured
      # rendering the configured number of times; any 🚩, unknown line, or
      # count mismatch flags.
      - name: Derive the verdicts from the recorded evidence, not the untrusted job's own report
        id: verdict
        env:
          CHECKS: ${{ inputs.checks }}
          REVIEW_CHECKS: ${{ inputs.review-checks }}
          REVIEW_VERDICTS: ${{ needs.update.outputs.review_verdicts }}
        run: |
          set -euo pipefail
          declare -A expected_count recorded_count
          while IFS= read -r cmd; do
            [ -z "$cmd" ] && continue
            expected_count["$cmd"]=$(( ${expected_count["$cmd"]:-0} + 1 ))
          done <<< "$CHECKS"
          failed=0
          # `|| [ -n "$line" ]` picks up a final line with no trailing
          # newline — checks.md as the update job writes it always ends in
          # one, but a forged or truncated file might not, and a plain
          # `while read` would silently drop (and never validate) that
          # last line.
          #
          # Each line is matched against the EXPECTED renderings built
          # from the trusted config — never parsed generically out of its
          # backtick delimiters. A configured command may itself contain
          # backticks (`echo \`date\``, legal shell), which the writer
          # embeds verbatim; a delimiter-parsing regex would refuse that
          # line and fail a batch whose check genuinely passed (Codex
          # review, on the rust-update twin). Fixed-string comparison per
          # configured command has no such blind spot, and an unmatched
          # line still fails closed.
          while IFS= read -r line || [ -n "$line" ]; do
            [ -z "$line" ] && continue
            matched=''
            for cmd in "${!expected_count[@]}"; do
              if [ "$line" = "- ✅ \`$cmd\`" ]; then
                matched=$cmd
                is_failed=0
                break
              fi
              prefix="- ❌ \`$cmd\` (exit "
              rest=${line#"$prefix"}
              if [ "$rest" != "$line" ] && [[ "$rest" =~ ^[0-9]+\)$ ]]; then
                matched=$cmd
                is_failed=1
                break
              fi
            done
            if [ -z "$matched" ]; then
              echo "::error::checks.md has a line that records no configured check in the canonical format — treating as failed rather than guessing: $line"
              echo 'passed=false' >> "$GITHUB_OUTPUT"
              echo 'flagged=true' >> "$GITHUB_OUTPUT"
              exit 0
            fi
            recorded_count["$matched"]=$(( ${recorded_count["$matched"]:-0} + 1 ))
            [ "$is_failed" -eq 1 ] && failed=1
          done < checks.md
          for cmd in "${!expected_count[@]}"; do
            if [ "${recorded_count[$cmd]:-0}" -ne "${expected_count[$cmd]}" ]; then
              echo "::error::checks.md records '$cmd' ${recorded_count[$cmd]:-0} time(s), expected ${expected_count[$cmd]} — treating as failed rather than guessing."
              echo 'passed=false' >> "$GITHUB_OUTPUT"
              echo 'flagged=true' >> "$GITHUB_OUTPUT"
              exit 0
            fi
          done
          if [ "$failed" -eq 1 ]; then
            echo 'passed=false' >> "$GITHUB_OUTPUT"
          else
            echo 'passed=true' >> "$GITHUB_OUTPUT"
          fi

          # The review flag, from the verdict record — pure canonical
          # lines, so unknown lines are refused the same way checks.md's
          # are. Only the EXACT configured renderings count, never a bare
          # '^- 🚩 ' prefix scan or a delimiter-parsing regex (Codex
          # review, twice over): the record carries no diff content to
          # false-flag on, and matching against the trusted config keeps a
          # configured command's own backticks inert.
          flagged=false
          if [ -n "$REVIEW_CHECKS" ]; then
            declare -A review_expected review_recorded
            while IFS= read -r cmd; do
              [ -z "$cmd" ] && continue
              review_expected["$cmd"]=$(( ${review_expected["$cmd"]:-0} + 1 ))
            done <<< "$REVIEW_CHECKS"
            flag_suffix_re='^[0-9]+\) — human review requested$'
            while IFS= read -r line || [ -n "$line" ]; do
              [ -z "$line" ] && continue
              vmatched=''
              for cmd in "${!review_expected[@]}"; do
                if [ "$line" = "- ✅ \`$cmd\`" ]; then
                  vmatched=$cmd
                  break
                fi
                prefix="- 🚩 \`$cmd\` (exit "
                rest=${line#"$prefix"}
                if [ "$rest" != "$line" ] && [[ "$rest" =~ $flag_suffix_re ]]; then
                  vmatched=$cmd
                  flagged=true
                  break
                fi
              done
              if [ -z "$vmatched" ]; then
                echo "::error::the review verdict record has a line that records no configured review check — flagging for human review rather than guessing: $line"
                flagged=true
              else
                review_recorded["$vmatched"]=$(( ${review_recorded["$vmatched"]:-0} + 1 ))
              fi
            done <<< "$REVIEW_VERDICTS"
            for cmd in "${!review_expected[@]}"; do
              if [ "${review_recorded[$cmd]:-0}" -ne "${review_expected[$cmd]}" ]; then
                echo "::error::the review verdict record has '$cmd' ${review_recorded[$cmd]:-0} time(s), expected ${review_expected[$cmd]} — flagging for human review rather than guessing."
                flagged=true
              fi
            done
          fi
          echo "flagged=$flagged" >> "$GITHUB_OUTPUT"

      # title, verdict, validated_head — travels as step outputs, the same
      # channel the fingerprints already ride.
      - name: Resolve the branch, then adopt or commit and push
        id: resolve
        env:
          GH_TOKEN: ${{ env.PAT || steps.app-token.outputs.token || github.token }}
          # Always GITHUB_TOKEN, never the App token: the Actions API read
          # below needs Actions:Read, which docs/GITHUB_APP.md deliberately
          # does not ask the App to have (Contents + pull requests only).
          # GITHUB_TOKEN always has it — it is the job that scheduled this
          # very run.
          DEFAULT_TOKEN: ${{ github.token }}
          CATALOG: ${{ inputs.catalog }}
          # As a git pathspec the catalog path is data too: `git add` with a
          # filename carrying pathspec syntax (`*`, `?`) would otherwise glob
          # and stage the restored report files alongside the catalog.
          GIT_LITERAL_PATHSPECS: '1'
          # Data, not script text — a quote or $() in a consumer's input must
          # never change what this write-enabled job executes.
          COMMIT_PREFIX: ${{ inputs.commit-prefix }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          # From THIS job's own "Derive the verdicts" step — computed from
          # fingerprint-verified checks.md and review.md — never the
          # untrusted update job's own report of itself. Env, not inline —
          # see the fingerprint step.
          PASSED: ${{ steps.verdict.outputs.passed }}
          REVIEW_FLAGGED: ${{ steps.verdict.outputs.flagged }}
          CATALOG_SHA: ${{ needs.update.outputs.catalog_sha }}
          REGENERATED_FILES: ${{ inputs.regenerated-files }}
          REGEN_SHA: ${{ needs.update.outputs.regen_sha }}
          REGEN_HASH: ${{ needs.update.outputs.regen_hash }}
          REGEN_MODE: ${{ needs.update.outputs.regen_mode }}
        run: |
          set -euo pipefail
          # Keyed on the RUN's date: the job runs weekly, so a month-scoped
          # name would collide with the previous week's branch. The date is
          # the run's immutable created_at, NOT the wall clock — a rerun
          # after UTC midnight would otherwise compute a different name,
          # miss the earlier attempt's branch, and open a duplicate PR for
          # the same batch. (run_started_at won't do: it moves to the
          # latest attempt.) GH_TOKEN overridden for just this command — see
          # DEFAULT_TOKEN above.
          created=$(GH_TOKEN="$DEFAULT_TOKEN" gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" --jq .created_at)
          today=${created%%T*}
          branch="deps/update-$today"

          # Empty by default (see the commit-prefix input) — only add the
          # trailing space when a consumer actually sets one, so a bare
          # title never picks up a leading space.
          prefix="$COMMIT_PREFIX"
          if [ -n "$prefix" ]; then
            prefix="$prefix "
          fi

          # No date in the title, deliberately. The title becomes the merge
          # commit's subject, and on the Android consumers that subject is a
          # bullet on the Play "What's new" card — where a run date is the
          # one part that varies and the one part a user cannot act on, so
          # it reads as a build stamp that leaked into the release notes.
          # Nothing is lost: the commit carries its own author date, the
          # branch is still `deps/update-$today`, and the PR records when it
          # opened. Don't add it back to tell weekly batches apart.
          #
          # A failing check outranks a review flag in the title: a red batch
          # is broken first, reviewable second. Both are reported in full in
          # the body either way, and EITHER condition holds auto-merge back.
          if [ "$PASSED" != 'true' ]; then
            title="${prefix}Update dependencies — CHECKS FAILING"
            verdict='**One or more checks failed.** Do not merge until they are fixed — the failures are real, not flakes to route around.'
          elif [ "$REVIEW_FLAGGED" = 'true' ]; then
            title="${prefix}Update dependencies — NEEDS HUMAN REVIEW"
            verdict='All checks passed, but a review check flagged this batch — see **Review** below. Auto-merge is deliberately not armed; merge by hand once the flagged change is acceptable.'
          else
            title="${prefix}Update dependencies"
            verdict='All checks passed in the job that produced this branch.'
          fi

          remote="https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git"

          # Never force-push: the PR body tells reviewers to push fixes to
          # this branch. If today's branch already exists there are two
          # cases, and a RERUN of this very run is the common one: adopt the
          # existing branch when its catalog is byte-identical to the
          # verified artifact (it is this run's own earlier attempt — reuse
          # it and its PR instead of duplicating them). Anything else is a
          # different batch, so take a run+attempt-scoped name of its own —
          # attempt-scoped so a rerun after a fallback push cannot collide
          # with the earlier attempt's fallback and be rejected.
          adopt=false
          if git ls-remote --exit-code --heads "$remote" "$branch" >/dev/null 2>&1; then
            git fetch --quiet "$remote" "refs/heads/$branch"
            # Adoption vouches for the WHOLE branch, so the whole branch is
            # what gets checked: its complete diff against the recorded base
            # must be exactly the catalog, that catalog must match the
            # verified fingerprint, the head must be exactly ONE commit
            # past the base — the shape this run's own earlier attempt
            # pushed — AND that commit's entire message must be the title
            # THIS run intends to merge. The tree checks alone would also
            # admit a change-plus-revert history whose net diff is clean
            # (a rebase merge would replay those junk commits into main),
            # and the message check covers the channel the tree checks
            # can't see: a rebase merge preserves the commit subject, so an
            # earlier failing attempt's "— CHECKS FAILING" marker, or a
            # message amended after the fact (same tree, same parent, no
            # prefix — straight past the consumer's release-note filter),
            # must refuse adoption and take a fresh branch with the right
            # subject instead. (`-z` so a path git would quote still
            # compares literally.)
            diff_paths=$(git diff -z --name-only HEAD FETCH_HEAD | tr '\0' '\n' | sed '/^$/d')
            # The catalog must be in the diff, and everything else in it
            # must be a declared regenerated file — nothing unexpected may
            # have moved.
            paths_ok=true
            printf '%s\n' "$diff_paths" | grep -Fxq -- "$CATALOG" || paths_ok=false
            while IFS= read -r p; do
              [ -z "$p" ] && continue
              [ "$p" = "$CATALOG" ] && continue
              if [ -z "$REGEN_SHA" ] || ! printf '%s\n' "$REGENERATED_FILES" | grep -Fxq -- "$p"; then
                paths_ok=false
                break
              fi
            done <<< "$diff_paths"
            # Every DECLARED regenerated file must match what THIS run
            # fingerprinted — checked against the full declaration, not
            # against diff_paths above. A file an EARLIER attempt left
            # unchanged from the base is absent from that diff, but a
            # `regenerate` command that reads external or time-dependent
            # data can still produce different bytes on THIS run; without
            # checking every declared path independently, that stale,
            # never-verified copy already on the branch would be adopted
            # anyway.
            if [ "$paths_ok" = true ] && [ -n "$REGEN_SHA" ]; then
              while IFS= read -r p; do
                [ -z "$p" ] && continue
                want=''
                # REGEN_HASH, not REGEN_SHA: a declared path containing a
                # backslash or newline makes sha256sum switch to its own
                # escaped line form (leading `\`, `\\`/`\n` inside the
                # name) — fine for `sha256sum -c` elsewhere, which decodes
                # it, but this hand lookup would never match the escaped
                # spelling and refuse an adoptable branch every time.
                # REGEN_HASH pairs each raw path with its hash alone, split
                # unambiguously on the first space since the hash is always
                # exactly 64 hex characters.
                while IFS= read -r line; do
                  [ -z "$line" ] && continue
                  if [ "${line#* }" = "$p" ]; then want=${line%% *}; fi
                done <<< "$REGEN_HASH"
                # --filters, not `git show`: `want` was fingerprinted from
                # the actual checked-out working-tree bytes, which a
                # consumer's .gitattributes (eol=crlf, a custom clean/smudge
                # filter) can differ from the raw blob `git show` reads.
                # `--filters` reproduces the same conversion a checkout
                # would apply, so it compares like with like; `git show`
                # would refuse an identical, correctly-regenerated file on
                # every rerun for a repo with such an attribute.
                if [ -z "$want" ] \
                   || [ "$(git cat-file --filters "FETCH_HEAD:$p" 2>/dev/null | sha256sum | cut -d' ' -f1)" != "$want" ]; then
                  paths_ok=false
                  break
                fi
                # Content can match while the mode doesn't: `git ls-tree`
                # reports the mode FETCH_HEAD actually committed for this
                # path (100644 / 100755 for a regular file), which is the
                # same two-state distinction the fingerprint records — an
                # adopted branch whose executable bit drifted from what
                # this run verified must not be waved through on content
                # alone.
                want_mode=''
                while IFS= read -r mode_line; do
                  [ -z "$mode_line" ] && continue
                  # Split only the fixed "MODE " prefix — `read` would trim
                  # a path's leading/trailing spaces and could match the
                  # wrong declared file.
                  mode_line_mode=${mode_line%% *}
                  mode_line_path=${mode_line#* }
                  if [ "$mode_line_path" = "$p" ]; then want_mode=$mode_line_mode; fi
                done <<< "$REGEN_MODE"
                have_mode=$(git ls-tree FETCH_HEAD -- "$p" | cut -d' ' -f1)
                if [ -z "$want_mode" ] || [ "$have_mode" != "$want_mode" ]; then
                  paths_ok=false
                  break
                fi
              done <<< "$REGENERATED_FILES"
            fi
            # --filters here too: CATALOG_SHA was fingerprinted from the
            # engine's checked-out working-tree write, same as the
            # regenerated-files fingerprints above — see that comment.
            if [ "$paths_ok" = true ] \
               && [ "$(git rev-list --count HEAD..FETCH_HEAD)" = "1" ] \
               && [ "$(git log -1 --format=%B FETCH_HEAD)" = "$title" ] \
               && [ "$(git cat-file --filters "FETCH_HEAD:$CATALOG" 2>/dev/null | sha256sum | cut -d' ' -f1)" = "$CATALOG_SHA" ]; then
              adopt=true
              # The exact commit adoption vouched for — auto-merge below is
              # bound to it, so a push landing after this fingerprint cannot
              # ride the arming.
              validated_head=$(git rev-parse FETCH_HEAD)
              echo "Adopting existing branch $branch — base plus exactly the verified catalog (a rerun)." >> "$GITHUB_STEP_SUMMARY"
            else
              # This run supersedes the earlier attempt: whatever armed
              # the existing branch's PR was computed by an older run, and
              # this run's verdict — a review flag the earlier attempt did
              # not have, say — may be exactly what should hold that batch
              # back. Disarm it before falling back, so an outdated arming
              # cannot merge past today's result; the fallback PR below
              # carries this run's own verdict and its own arming.
              # Non-fatal — it errors when auto-merge was never armed.
              oldpr=$(gh api "repos/${GITHUB_REPOSITORY}/pulls?head=${GITHUB_REPOSITORY_OWNER}:${branch}&state=open" \
                --jq '[.[] | select(.base.ref == env.DEFAULT_BRANCH)][0].number // empty')
              if [ -n "$oldpr" ]; then
                # The disarm and the verify are separate on purpose:
                # --disable-auto exits nonzero both when auto-merge was
                # never armed (the common case, fine) and when the API
                # call failed (an armed PR still armed — not fine), so
                # the exit code proves nothing. Only reading the state
                # back does, and a state that cannot be read or is still
                # armed is a STOP: continuing would leave a superseded PR
                # able to merge past this run's verdict.
                gh pr merge --disable-auto "$oldpr" >/dev/null 2>&1 || true
                armed=$(gh pr view "$oldpr" --json autoMergeRequest --jq '.autoMergeRequest != null') || {
                  echo "::error::could not confirm the auto-merge state of superseded PR #$oldpr — not continuing while it may still be armed."
                  exit 1
                }
                if [ "$armed" != 'false' ]; then
                  echo "::error::auto-merge on superseded PR #$oldpr could not be disarmed — not continuing while it is armed."
                  exit 1
                fi
                echo "Auto-merge on PR #$oldpr is confirmed disarmed — its branch is superseded by this run." >> "$GITHUB_STEP_SUMMARY"
              fi
              branch="$branch-run${{ github.run_number }}-${{ github.run_attempt }}"
              echo "Today's branch already exists with different content; using $branch instead." >> "$GITHUB_STEP_SUMMARY"
            fi
          fi

          if [ "$adopt" != true ]; then
            # Belt and braces on a fresh runner where nothing has run.
            git config --global core.hooksPath /dev/null
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git switch -c "$branch"
            # Stage by name: the artifact only carries these files, but naming
            # them keeps that true even if it ever carries more. Staging an
            # unchanged regenerated file is a no-op, so the batch commits
            # only what actually moved.
            git add -- "$CATALOG"
            if [ -n "$REGEN_SHA" ]; then
              while IFS= read -r f; do
                [ -z "$f" ] && continue
                git add -- "$f"
              done <<< "$REGENERATED_FILES"
            fi
            git commit -q -m "$title"
            # Verify what was actually just committed, not just what was
            # staged: a Git attribute like `ident` is not round-trip-safe —
            # its smudge output embeds the blob's own hash, which only
            # exists once the commit does, so a checkout of what was just
            # committed can differ from what regeneration fingerprinted
            # even though nothing tampered with anything in between. Same
            # comparisons the rerun-adoption loop above already makes,
            # applied to the commit about to be pushed instead of an
            # existing branch's FETCH_HEAD: a stop here, not a silent push
            # of content nothing actually verified end to end.
            if [ "$(git cat-file --filters "HEAD:$CATALOG" | sha256sum | cut -d' ' -f1)" != "$CATALOG_SHA" ]; then
              echo '::error::The committed catalog does not match what the update job fingerprinted — refusing to push unverified content.'
              exit 1
            fi
            if [ -n "$REGEN_HASH" ]; then
              while IFS= read -r f; do
                [ -z "$f" ] && continue
                want=''
                while IFS= read -r line; do
                  [ -z "$line" ] && continue
                  if [ "${line#* }" = "$f" ]; then want=${line%% *}; fi
                done <<< "$REGEN_HASH"
                if [ -z "$want" ] \
                   || [ "$(git cat-file --filters "HEAD:$f" | sha256sum | cut -d' ' -f1)" != "$want" ]; then
                  echo "::error::The committed file '$f' does not match what regeneration fingerprinted — refusing to push unverified content."
                  exit 1
                fi
              done <<< "$REGENERATED_FILES"
            fi
            git push "$remote" "HEAD:refs/heads/$branch"
            validated_head=$(git rev-parse HEAD)
          fi

          # title embeds $COMMIT_PREFIX, a consumer input — a `key=value`
          # write for it is not safe. A prefix holding an embedded newline
          # followed by a line that reads like another output ("branch=…")
          # is interpreted as exactly that: it overwrites the branch this
          # run actually resolved, and the next step queries, disarms,
          # edits, and dispatches CI for whatever branch that line named
          # instead — reproduced this precisely (a crafted commit-prefix
          # silently replaced $GITHUB_OUTPUT's branch value). The heredoc
          # form treats everything between the two delimiter lines as one
          # literal value regardless of what it contains; the delimiter
          # itself is randomized per run so nothing in a consumer's input
          # can be crafted in advance to match it and break out early.
          delim="EOF_${GITHUB_RUN_ID}_${RANDOM}${RANDOM}"
          {
            echo "branch<<$delim"
            printf '%s\n' "$branch"
            echo "$delim"
            echo "title<<$delim"
            printf '%s\n' "$title"
            echo "$delim"
            echo "verdict<<$delim"
            printf '%s\n' "$verdict"
            echo "$delim"
            echo "validated_head<<$delim"
            printf '%s\n' "$validated_head"
            echo "$delim"
            echo "adopt<<$delim"
            printf '%s\n' "$adopt"
            echo "$delim"
          } >> "$GITHUB_OUTPUT"

      - name: Open the pull request
        env:
          GH_TOKEN: ${{ env.PAT || steps.app-token.outputs.token || github.token }}
          CI_WORKFLOW: ${{ inputs.ci-workflow }}
          # Whether THIS run authored with a real collaborator's identity —
          # a PAT or a minted App token — rather than GITHUB_TOKEN. Not
          # whether the pull request it ends up using was actually opened
          # with one. An adopted rerun can reuse a PR an earlier, default-
          # token attempt opened; the script below derives the real
          # nondefault_opened_pr from this plus whether the resolve step
          # pushed or this step creates the PR itself.
          NONDEFAULT_TOKEN_USED: ${{ env.PAT != '' || steps.app-token.outputs.token != '' }}
          # Always GITHUB_TOKEN, never the App token — see the resolve
          # step's DEFAULT_TOKEN comment. Used here for the Actions-API
          # dispatch and the branch-rules probe, neither of which the App's
          # two permissions (Contents + pull requests) cover.
          DEFAULT_TOKEN: ${{ github.token }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          PASSED: ${{ steps.verdict.outputs.passed }}
          REVIEW_FLAGGED: ${{ steps.verdict.outputs.flagged }}
          RESOLVED_BRANCH: ${{ steps.resolve.outputs.branch }}
          RESOLVED_TITLE: ${{ steps.resolve.outputs.title }}
          RESOLVED_VERDICT: ${{ steps.resolve.outputs.verdict }}
          RESOLVED_HEAD: ${{ steps.resolve.outputs.validated_head }}
          RESOLVED_ADOPT: ${{ steps.resolve.outputs.adopt }}
        run: |
          set -euo pipefail
          branch="$RESOLVED_BRANCH"
          title="$RESOLVED_TITLE"
          verdict="$RESOLVED_VERDICT"
          validated_head="$RESOLVED_HEAD"
          adopt="$RESOLVED_ADOPT"

          # A PR opened under GITHUB_TOKEN's identity DOES trigger a
          # consumer's own `on: pull_request` workflows, same as any other —
          # but GitHub gates that run pending manual approval, since
          # GITHUB_TOKEN's identity (github-actions[bot]) is not a
          # repository collaborator, and nothing runs until someone clicks
          # approve (docs/PAT.md, docs/GITHUB_APP.md). Dispatch sidesteps the gate: ask
          # for one explicitly against the branch just pushed, naming the PR
          # so the consumer's docs lane can bind the run to it. Only a PR
          # still based on the default branch may be reused: the adoption
          # checks compared the branch's tree against that base, so a PR
          # someone retargeted would merge the verified catalog into a
          # branch nobody validated against. A retargeted PR is left alone
          # and a fresh, correctly-based one is opened beside it (GitHub
          # allows one open PR per head+base pair, so the create succeeds).
          pr_opened_here=false
          pr=$(gh api "repos/${GITHUB_REPOSITORY}/pulls?head=${GITHUB_REPOSITORY_OWNER}:${branch}&state=open" \
            --jq '[.[] | select(.base.ref == env.DEFAULT_BRANCH)][0].number // empty')
          if [ -z "$pr" ]; then
            pr_url=$(gh pr create --title "$title" \
              --body 'Opening — the checks report lands in a moment.' \
              --base "$DEFAULT_BRANCH" --head "$branch")
            pr=${pr_url##*/}
            pr_opened_here=true
          fi

          # NONDEFAULT_TOKEN_USED says only that THIS invocation had a PAT
          # or minted App token available — not that the pull request open
          # above actually used it. An adopted rerun (adopt=true) reuses
          # whatever PR an EARLIER attempt already opened, possibly under
          # GITHUB_TOKEN if the credential was added between attempts, with
          # no push and no `gh pr create` of its own to carry the real
          # identity. Only trust the native trigger when this run itself did
          # one of the two things that puts that identity on the wire:
          # pushed the branch fresh (adopt != true, in the resolve step) or
          # created the pull request here (pr_opened_here). Otherwise fall
          # back to dispatching same as the GITHUB_TOKEN case — a possibly-
          # redundant CI run is far cheaper than one that silently never
          # starts.
          nondefault_opened_pr=false
          if [ "$NONDEFAULT_TOKEN_USED" = 'true' ] && { [ "$adopt" != true ] || [ "$pr_opened_here" = true ]; }; then
            nondefault_opened_pr=true
          fi

          # Clear any stale auto-merge from a prior attempt NOW, before the
          # CI dispatch below: a required check going green while this step
          # is still editing the body would otherwise merge on an arming
          # this attempt never made. From here to the arming at the bottom,
          # the PR has no auto-merge at all; the passing path re-arms it,
          # pinned to the validated head. Same disarm-then-verify shape as
          # the superseded-branch case above: --disable-auto's exit code
          # cannot distinguish "was never armed" from "API call failed",
          # so only reading the state back proves the PR is unarmed — and
          # an unconfirmable or still-armed state is a stop, because this
          # run may be about to decide the batch must NOT merge.
          gh pr merge --disable-auto "$pr" >/dev/null 2>&1 || true
          armed=$(gh pr view "$pr" --json autoMergeRequest --jq '.autoMergeRequest != null') || {
            echo "::error::could not confirm the auto-merge state of PR #$pr — not continuing while a stale arming may survive."
            exit 1
          }
          if [ "$armed" != 'false' ]; then
            echo "::error::a stale auto-merge on PR #$pr could not be disarmed — not continuing while it is armed."
            exit 1
          fi

          ci_started=false
          if [ "$nondefault_opened_pr" != 'true' ] && [ -n "$CI_WORKFLOW" ]; then
            # GH_TOKEN overridden to $DEFAULT_TOKEN here too, same as the
            # actions/runs read in the resolve step: this branch runs
            # whenever a PAT or App token was available but didn't open the
            # PR (an adopted rerun of a default-token PR), so the ambient
            # $GH_TOKEN can still be that credential — and docs/PAT.md and
            # docs/GITHUB_APP.md both scope their credential to Contents +
            # pull requests only, deliberately excluding Actions:write, so
            # neither can dispatch a workflow.
            if GH_TOKEN="$DEFAULT_TOKEN" gh workflow run "$CI_WORKFLOW" --ref "$branch" -f pr="$pr"; then
              ci_started=true
            else
              echo "Could not start CI on $branch; the PR body says so." >> "$GITHUB_STEP_SUMMARY"
            fi
          fi

          # regen-stat.txt is the one piece of this body with no size
          # guarantee: report.md, checks.md, review.md and deps-stat.txt are
          # each bounded by the batch's own shape (review.md by its own
          # budget above), but regen-stat.txt scales with how many files a
          # consumer declares in regenerated-files — arbitrarily large from
          # this workflow's point of view. Cap it against whatever space is
          # actually left, so a large declaration can never push body.md
          # past GitHub's 65,536-character limit; without this, `gh pr edit`
          # fails all three retries below, the PR keeps its placeholder
          # body, and — since PASSED/REVIEW_FLAGGED are unaffected — the
          # auto-merge arm at the bottom still fires on it regardless.
          other=$(( $(wc -c < report.md) + $(wc -c < checks.md) \
                     + $(wc -c < review.md) + $(wc -c < deps-stat.txt) ))
          regen_budget=$(( 65536 - 6000 - other ))
          if [ "$regen_budget" -lt 0 ]; then regen_budget=0; fi
          if [ "$(wc -c < regen-stat.txt)" -gt "$regen_budget" ]; then
            head -c "$regen_budget" regen-stat.txt > "$RUNNER_TEMP/regen-stat.trunc"
            {
              echo
              echo "… truncated — the full diffstat is $(wc -c < regen-stat.txt) bytes; see this pull request's own Files Changed tab for the complete regenerated-file diff."
            } >> "$RUNNER_TEMP/regen-stat.trunc"
            mv -- "$RUNNER_TEMP/regen-stat.trunc" regen-stat.txt
          fi

          {
            echo 'Every version catalog entry moved to the newest stable release **within'
            echo 'its current major**. No majors: those need a deliberate migration and'
            echo 'this job cannot produce one. (Transitives are resolved by Gradle from'
            echo 'these declared versions — the checks below are the coverage there.)'
            echo
            cat report.md
            echo '## Checks'
            echo
            cat checks.md
            echo
            if [ -s review.md ]; then
              echo '## Review'
              echo
              cat review.md
              echo
            fi
            echo "$verdict"
            echo
            # Nothing prints for a native `on: pull_request` trigger
            # (nondefault_opened_pr) or a successful dispatch (ci_started) —
            # in both cases CI is already running and the checks report says
            # so. Only the case where neither started CI needs a reviewer's
            # attention.
            if [ "$nondefault_opened_pr" != 'true' ] && [ "$ci_started" != true ]; then
              echo '> **CI was not started on this branch by this job, so the checks above'
              echo '> are the only ones that ran here.** Check this pull request'"'"'s checks'
              echo '> tab for one waiting on manual approval, or push any commit to the branch'
              echo '> and CI runs normally from then on.'
            fi
            echo
            echo '## Changed'
            echo
            echo '```'
            cat deps-stat.txt
            echo '```'
            echo
            if [ -s regen-stat.txt ]; then
              echo '## Regenerated'
              echo
              echo 'Derived from the new dependency set and committed with it:'
              echo
              echo '```'
              cat regen-stat.txt
              echo '```'
              echo
            fi
            echo '---'
            echo '_Opened by the weekly gradle-update workflow._'
          } > body.md

          # A transient API failure here must not strand the PR on its
          # placeholder body under `set -e` — the assignment below still has
          # to run, because the assignee is how an unattended PR gets seen.
          edited=false
          for _ in 1 2 3; do
            if gh pr edit "$pr" --title "$title" --body-file body.md; then
              edited=true
              break
            fi
            sleep 5
          done
          if [ "$edited" != true ]; then
            echo "::warning::Could not publish the PR body for #$pr; it still carries the placeholder."
          fi

          # Put it in front of a human — an unattended weekly PR nobody is
          # assigned to is exactly the PR that goes unnoticed. Non-fatal: an
          # opened-but-unassigned PR is recoverable.
          reviewer='${{ github.repository_owner }}'
          if ! gh pr edit "$pr" --add-assignee "$reviewer" --add-reviewer "$reviewer"; then
            echo "Could not assign or request review from @$reviewer — the PR is open and needs a look." \
              >> "$GITHUB_STEP_SUMMARY"
          fi

          # mesh#533, the first PR rust-update's copy of this workflow ever
          # opened under the App's identity, got no automatic Codex review —
          # the connector's webhook trigger apparently doesn't fire for an
          # App-authored PR the way it does for a human or GITHUB_TOKEN one.
          # A required `codex` status that never gets set leaves the PR
          # silently stuck until someone notices, which defeats the point of
          # the merge gate, so nudge it explicitly whenever this run opened
          # the PR under a non-default identity — a PAT's is a real user
          # account rather than an App or a bot, so it likely gets the
          # native trigger a human-opened PR does, but that's unconfirmed and
          # the nudge is harmless when redundant, so it fires either way
          # rather than betting the merge gate on an assumption. Retried like
          # the body edit above: a rerun of THIS attempt doesn't get another
          # chance, since a later rerun adopts the existing PR (adopt=true,
          # pr_opened_here=false) and nondefault_opened_pr goes false with it
          # — so a nudge that never leaves this loop is not recoverable
          # except by a human noticing and commenting manually. Harmless if
          # Codex reacts on its own first either way — the nudge just asks
          # for a second look.
          if [ "$nondefault_opened_pr" = 'true' ]; then
            nudged=false
            for _ in 1 2 3; do
              if gh pr comment "$pr" --body '@codex review'; then
                nudged=true
                break
              fi
              sleep 5
            done
            if [ "$nudged" != true ]; then
              echo "Could not post the @codex review nudge on #$pr after 3 attempts — it needs a manual \`@codex review\` comment." \
                >> "$GITHUB_STEP_SUMMARY"
            fi
          fi

          # Arm auto-merge so a clean batch lands unattended; the ruleset's
          # required checks do the reviewing. ONLY when this run's own checks
          # passed: auto-merge waits for the ruleset's requirements, and a
          # consumer whose ruleset requires little (or whose CI dispatch
          # failed, leaving nothing red on the PR) would otherwise merge a
          # batch this very workflow titled CHECKS FAILING. A failing batch
          # stays open, assigned, and waits for a human — and so does a
          # review-flagged one: "needs human review" means no unattended
          # merge, whatever the checks said. Non-fatal: repos without
          # auto-merge allowed just fall back to manual.
          #
          # And only where the ruleset requires branches up to date (strict
          # required status checks). Auto-merge can fire long after this job
          # ends, the base-moved check above ran once, and
          # --match-head-commit closes only the ARMING-time race (a
          # concurrent push between the fingerprint and this call) — it is
          # not a binding stored on the auto-merge request, so it cannot
          # veto a head that changes later. Without the strict policy, a
          # base advancing while auto-merge waits would merge this catalog
          # against a tree it was never validated with. WITH it, the merge
          # blocks until someone with write access updates the branch, and
          # the merged tree must then pass the ruleset's required checks
          # before auto-merge fires — the revalidation is the consumer's
          # own required CI, which is why the ruleset must require it. (A
          # push by a user without write access disables auto-merge
          # outright, GitHub's own rule.) The policy is verified rather
          # than assumed, and an unreadable answer counts as absent: fail
          # closed to a manual merge. (Effective rules come from the
          # rulesets API; the ruleset is how these repos protect main.)
          # The rules list is paginated; --paginate --slurp gathers every
          # page into one array of pages, so a rule past page one cannot
          # read as "policy absent". GH_TOKEN overridden to $DEFAULT_TOKEN
          # here too, same as the Actions-API sites above: this is a read no
          # write is riding on, and docs/GITHUB_APP.md deliberately keeps
          # the App to two permissions (Contents + pull requests) rather
          # than adding a third just to cover this one diagnostic query —
          # GITHUB_TOKEN already reads it today, unmodified by this port.
          strict=$(GH_TOKEN="$DEFAULT_TOKEN" gh api --paginate --slurp "repos/${GITHUB_REPOSITORY}/rules/branches/$(printf %s "$DEFAULT_BRANCH" | jq -sRr @uri)" \
            | jq '[.[][] | select(.type == "required_status_checks")
                         | .parameters.strict_required_status_checks_policy] | any') || strict=false
          if [ "$PASSED" = 'true' ] && [ "$REVIEW_FLAGGED" != 'true' ] && [ "$strict" = 'true' ]; then
            # Any stale arming was cleared right after the PR was identified,
            # so the only arming that can exist after this line is one bound
            # to the exact commit this run validated (the adopted FETCH_HEAD
            # or the commit just pushed): a concurrent push to the branch
            # between the fingerprint and this call would otherwise be what
            # a thin ruleset auto-merges. With the pin, that race makes this
            # command fail — and then the PR simply has no auto-merge and
            # waits for a human.
            if ! gh pr merge --auto --rebase --match-head-commit "$validated_head" "$pr"; then
              echo 'Could not arm auto-merge — the PR is open and waits for a manual merge.' \
                >> "$GITHUB_STEP_SUMMARY"
            fi
          else
            # No arming here, and none survives from before: a prior
            # attempt's request was cleared before CI was dispatched, so the
            # required checks going green cannot merge a PR titled
            # CHECKS FAILING. The summary names which condition held it
            # back — a review flag is not a failure, a missing strict policy
            # is neither, and saying "failed" for either would send whoever
            # reads the run down the wrong path.
            if [ "$PASSED" != 'true' ]; then
              echo 'Checks failed, so auto-merge was deliberately not armed (and any earlier arming was cleared).' >> "$GITHUB_STEP_SUMMARY"
            elif [ "$REVIEW_FLAGGED" = 'true' ]; then
              echo 'A review check flagged this batch for human review, so auto-merge was deliberately not armed (and any earlier arming was cleared).' >> "$GITHUB_STEP_SUMMARY"
            else
              echo 'Auto-merge was not armed: the base branch ruleset does not require branches to be up to date, so a base advance while auto-merge waited could merge this catalog against a tree it was never resolved with. Merge manually, or add the strict status-check policy to the ruleset.' >> "$GITHUB_STEP_SUMMARY"
            fi
          fi
